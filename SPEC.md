# torrcast — ТЗ MVP

Проект **torrcast**, команда — **`cast`**. Консольная утилита: находит фильм/сериал по
названию (понимая франшизы и кривые запросы), даёт выбрать релиз и озвучку, стримит
торрент на ТВ через Chromecast — без скачивания, без облака в пути данных. Идейный
наследник kinocast, но каталог — торрент-трекеры, а поток не покидает LAN.

**Установка = три действия, ноль регистраций:**

```
git clone <репа torrcast> && cd torrcast
./install.sh                      # ставит зависимости, TorrServer, Prowlarr, юниты
cast --tv 192.168.100.102         # единственная настройка — адрес ТВ
```

Никаких внешних API-ключей и учёток. Все внутренние ключи (Prowlarr↔cast)
`install.sh` генерит сам.

> ⚠️ Конфликт имён: на agent-ops алиас `cast` сейчас указывает на kinocast. На стенде
> и в LXC torrcast команда своя; переключение алиаса на agent-ops — отдельное решение
> владельца после приёмки, не в MVP.

---

## 1. Анти-цели (уроки кинокаста)

Кинокаст переусложнён, потому что боролся с чужим CDN и чужим геоблоком. У torrcast
этих врагов нет — и защит от них быть не должно:

- **Ноль Samsung-специфики.** Никаких PowerState, анти-wakeup, nudge-сторожей.
  Просто каст на Chromecast.
- **Ноль облака.** Ни в пути данных, ни в метаданных. Поток: стенд/LXC → ТВ, LAN-only.
- **Ноль внешних сервисов с ключами.** TMDB/Kinopoisk-API не используем — метаданные
  строим из выдачи трекеров (см. §3).
- **Ноль своего CDN-кода.** Стримит TorrServer, пакует ffmpeg, кастит catt. Наш код —
  клей и UX.
- **Ноль молчаливых fallback'ов.** Не нашли / не срослось → одна честная строка почему.
  Никогда не кастим «что-то похожее» без подтверждения.
- **Ноль .bak на проде.** Деплой только репа→прод.
- Телеграм-бот, голос, MCP, веб — не в MVP.

## 2. UX — сценарии (это и есть продукт)

### 2.1 Фильм из франшизы

```
$ cast тачки
«Тачки» — найдено:
  1. Тачки (2006)
  2. Тачки 2 (2011)
  3. Тачки 3 (2017)
Что смотрим? [1]: 1

Релизы:
  №  Качество  Размер   Сиды  Озвучка              Кодек
  1  1080p     8.2 ГБ   214   Дубляж               H.264
  2  1080p     4.1 ГБ   120   Дубляж + Original    H.264
  3  2160p     28 ГБ    45    Дубляж, HDR          HEVC ⚠
Какой берём? [1]: 2

Дорожки: 1. Русский (дубляж)  2. English
Озвучка? [1]: 1

▶ «Тачки» (2006) · 1080p · дубляж → ТВ
```

- Дефолты в скобках: Enter на каждом вопросе = разумный выбор (самый обсиженный
  релиз, русская дорожка). Счастливый путь — три Enter'а.
- Один результат — меню франшизы пропускается. Одна дорожка в файле — вопрос
  про озвучку пропускается.

### 2.2 Номер = позиция во франшизе

`cast матрица 2` → второй фильм франшизы («Матрица: Перезагрузка»), даже если
в реальном названии нет двойки. Франшиза = кластер найденных картин с общим
каноническим названием, отсортированный по году; номер — индекс в нём.

### 2.3 Возобновление

`cast тачки` при недосмотренном прогрессе:

```
«Тачки» остановились на 0:41:07. Продолжить? [Да/сначала]:
```

Релиз и дорожка берутся из сохранённого выбора — вопросы не повторяются.

### 2.4 Сериалы

```
$ cast киберпанк
  1. Киберпанк: Бегущие по краю (2022, сериал)
  2. ...
Что смотрим? [1]: 1
[выбор релиза сезона + дорожки — один раз, запоминается]
▶ s1e1 → ТВ
```

- Список серий = файлы раздачи (имена парсятся: s01e05 и варианты).
- Прогресс на серию: позиция ≥ 95% длительности → досмотрена → следующая.
- `cast киберпанк` без аргументов → продолжает нужную серию с нужного места.
- Автопереход: серия кончилась → сам кастит следующую (следующий файл раздачи).
- Явное указание: `cast киберпанк s2e5` (также `2x5`, «2 сезон 5 серия»).

### 2.5 Управление

```
cast stop      # снять каст, зафиксировать позицию
cast status    # что играет, позиция/длительность, источник
```

Всё. Пауза и перемотка — пультом ТВ (стандартный media receiver их умеет).

## 3. Архитектура

```
запрос ──► search: Prowlarr (Torznab, публичные индексеры) ──► сырые релизы
       ──► parse:  парсер имён релизов/файлов ──► канон. название, год, качество,
                   озвучка, кодек ──► кластеризация в франшизы/сериалы
       ──► stream: TorrServer ──► HTTP-поток выбранного файла (кэш в RAM)
       ──► pack:   ffmpeg: видео copy, аудио → AAC, выбранная дорожка ──► HLS
       ──► serve:  https://torrcast.anysda.space — ЛОКАЛЬНО на стенде (caddy/nginx,
                   LE-серт; split-DNS в AdGuard → LAN-адрес; наружу не публикуем.
                   ⚠️ home-ingress/Traefik НЕ использовать — это облачная VPS,
                   поток через неё покинул бы LAN)
       ──► cast:   catt ──► Chromecast ТВ (адрес из конфига)
```

Ключевые решения:

- **Метаданные — из самих трекеров, без внешних API.** Имена русских раздач
  («Матрица / The Matrix (1999) BDRip 1080p …») парсятся в структуру
  (название, оригинал, год, качество, озвучка, кодек); картины кластеризуются по
  каноническому названию → это и есть «франшиза», сортировка по году даёт нумерацию.
  Парсер — ключевой модуль, обкатывается на корпусе реальной выдачи (§7, этап 1).
- **Поиск — свой Prowlarr, индексеры без учёток**: RuTor + публичные
  (Jacred мёртв — проверено 04-08-2026 с двух egress). Учётки RuTracker/Kinozal —
  опциональное обогащение потом, НЕ требование установки. Доступ к блокированным
  трекерам — через обход DPI, который уже есть в LAN (zapret-контур), либо
  прямые зеркала; решается на этапе 1 по факту.
- **Один наш процесс — CLI.** Постоянных демонов своих нет. На время воспроизведения
  CLI поднимает transient-юнит через `systemd-run` (`torrcast-play`): ffmpeg-упаковка +
  мини-сторож (раз в 10 с снимает позицию с приёмника → state; конец серии →
  автопереход). `cast stop` гасит юнит. Логи бесплатно в journald.
- **Приёмник — за интерфейсом с двумя реализациями**: `chromecast` (catt/pychromecast)
  и `mock` (headless ffmpeg-клиент: тянет HLS как ТВ, декодирует, отдаёт позицию).
  Mock — не заглушка «для галочки», а полноценный приёмник для автономной приёмки
  (§7): на нём проверяются resume, порог 95% и автопереход серий.
- **TorrServer — кэш в RAM**, на диск не пишем.
- **Пиры — magnet + DHT + публичные ретрекеры.** P2p-трафик РКН не режет, VPN в пути
  данных нет.
- **HEVC:** релизы показываем с пометкой ⚠, дефолт — лучший H.264. Ест ли ресивер
  Q70D HEVC — узнаем только на живом тесте владельца; до тех пор пометка остаётся.
- **Дорожки** читаем `ffprobe` прямо из HTTP-потока TorrServer (заголовок mkv по
  Range, качать файл не надо).
- **Формат потока на ТВ — фиксирован, без вариаций:** HLS, сегменты MPEG-TS ~4 с,
  один вариант в манифесте; видео `copy` (H.264), аудио **всегда** перекодируется
  в AAC stereo 192k (никаких passthrough AC3/DTS — лотерея на ресивере);
  CORS `Access-Control-Allow-Origin: *` на всех ответах (Chromecast без него
  молча не играет); практический потолок битрейта **~20 Мбит/с** (урок Q70D
  из kinocast) — релизы жирнее помечаются в меню.
- **Перемотка/resume = рестарт ffmpeg с `-ss <pos>`.** Живой ремукс не умеет
  честный seek по всему файлу; манифест всегда «с нуля», позиция задаётся
  входом ffmpeg. Просто, предсказуемо, укладывается в бюджет §3.1.
- **HTTPS-серт для LAN-хоста** (`torrcast.anysda.space` → приватный IP):
  HTTP-01 не сработает, нужен **DNS-01** (у владельца уже есть LE-контур на
  amnezia для `cast.anysda.space` — посмотреть, как выпущен, и переиспользовать
  механику) либо перенос серта с публичной машины renewal-хуком. Выбор — на
  этапе 2, с обоснованием. Self-signed НЕ вариант — Chromecast не примет.

## 3.1 Латентность старта (зафиксированное ожидание)

Холодный старт: **15–30 с** от последнего Enter'а до картинки (поиск 3–10 с,
DHT+метаданные 5–20 с, буфер+ffprobe 3–10 с, HLS+запуск приёмника 3–5 с).
Resume: **5–15 с** (поиск и меню пропущены, только добор кусков). «5 секунд» не
обещаем — это физика DHT/буферизации; дисперсию определяют сиды, поэтому дефолт
всегда — самый обсиженный релиз.

**Обязательный трюк — прогрев под меню:** как только известен топ-релиз, его magnet
в фоне уходит в TorrServer, пока пользователь отвечает на вопросы (дорожки для меню
озвучек всё равно читаются из потока). К последнему Enter'у DHT и буфер уже тёплые.

## 4. Состояние

`/var/lib/torrcast/state.json`, атомарная запись (tmp + rename):

```json
{
  "movie:матрица:1999": {
    "title": "Матрица", "magnet": "…", "file_idx": 0, "audio": 1,
    "pos": 2467, "dur": 8160, "updated": "2026-08-04T23:41:00+03:00"
  },
  "tv:киберпанк-бегущие-по-краю:2022": {
    "magnet": "…", "audio": 0, "season": 1, "episode": 3,
    "pos": 310, "dur": 1440, "updated": "…"
  }
}
```

Ключ — тип + slug канонического названия + год (внешних ID у нас нет). Выбор релиза
и дорожки — часть состояния: стык серий и resume не задают вопросов заново.
`--new` сбрасывает запись и проходит выбор с нуля.

## 5. CLI-контракт

```
cast <запрос> [sNeM] [--new] [--release N] [--audio N] [--dry]
cast stop
cast status
cast --tv <ip>        # разовая настройка адреса ТВ (пишет конфиг)
```

- `--release/--audio` — обход меню для скриптов и повторов.
- `--dry` — вся цепочка резолва и выбора без каста (отладка, этап 1).
- Коды выхода: `0` ок · `1` не нашли (с ближайшими вариантами) · `2` инфра-ошибка
  (одна строка: что именно легло — Prowlarr / TorrServer / приёмник).

Конфиг `/etc/torrcast/config.json`: единственное обязательное поле — адрес ТВ.
Всё остальное имеет рабочие дефолты.

## 6. Код и практики

- Python 3.12, пакет из 6 модулей: `cli` · `parse` · `search` · `stream` · `cast` ·
  `state`. Целевой объём **≤ ~1200 строк** — рост сверх = сигнал, что тащим лишнее.
- Зависимости: `requests`, `catt`; парсер имён — поверх готовой библиотеки (guessit),
  если она реально сокращает код, иначе свой компактный.
- Типы везде, `mypy --strict` и `ruff` зелёные с первого коммита.
- Парсер имён покрыт тестами на фикстурах из реальной выдачи трекеров.
- Сообщения об ошибках — короткие, по-русски, без трейсбеков наружу.
- `install.sh` идемпотентен (повторный запуск ничего не ломает).
- Деплой: репа → стенд, всегда. Правок руками на стенде не существует.

## 6.1 Модель исполнения (оркестрация) — ОБЯЗАТЕЛЬНО

Разработку ведёт **Fable 5 как оркестратор**. Оркестратор руками НЕ кодит,
НЕ дебажит и НЕ ковыряет стенд — его контекст это контекст главного разраба,
и он должен дожить до конца проекта.

- **Вся работа делается субагентами на Opus 5** (Agent tool, `model: opus`):
  код, тесты, отладка, снятие корпуса имён, развёртывание стенда, прогоны приёмки.
- Оркестратору остаётся: декомпозиция этапов, постановка задач, ревью результатов,
  ведение чек-листа §7.5, справка §9 и общение с владельцем.
- Причина, дословно: сохранность контекста главной сессии и экономия токенов —
  иначе сессия завершится посреди работы, и проект осиротеет.
- Субагенту ставится **цель + ограждения, не рецепт**; свои ошибки он дебажит сам
  и возвращает результат, а не лог мучений.
- Параллельные субагенты разводятся по живым объектам (стенд/репа/vault);
  в одной репе — только через git worktree.

## 7. Этапы

1. **Цепочка данных без ТВ.** `cast --dry матрица 2` печатает: поиск → парс →
   кластеры-франшизы → релизы → дорожки из ffprobe. Здесь же: снять корпус реальных
   имён раздач (RuTor и др.) → фикстуры для парсера; решить вопрос доступа к
   индексерам из LAN (zapret/зеркала).
2. **Каст фильма end-to-end на mock-приёмник.** ffmpeg→HLS, локальный https
   (серт по §3) + split-DNS, mock тянет по https и декодирует. Приёмка: «Моана 2»
   декодируется mock'ом от начала до конца, кодеки h264+aac, сегменты без дыр.
3. **Прогресс.** transient-юнит, снятие позиции, resume, порог 95% — всё на mock.
4. **Сериалы.** sNeM-парсер, автопереход, кэш выбора — всё на mock.
5. **Тестовый стенд в Proxmox — автономная приёмка.** LXC на caffeine
   (VMID из тестового диапазона 500–1000), деплой строго из репы:
   `git clone → ./install.sh → cast --tv <mock>`. Полный прогон сценариев §2 на
   mock-приёмнике, **телевизор не включается вообще**. Чек-лист приёмки:
   - [ ] установка с нуля тремя командами, ноль ручных шагов и регистраций
   - [ ] `cast матрица 2` → «Перезагрузка» в --dry
   - [ ] фильм: старт ≤ 30 с, mock декодирует без дыр
   - [ ] resume фильма с сохранённой позиции
   - [ ] сериал: s1e1 → 95% → автопереход s1e2 → resume после stop
   - [ ] `stop`/`status` корректны, transient-юнит не течёт
   - [ ] https валиден: LE-серт не self-signed, split-DNS резолвит, CORS на месте
6. **Подключение к реальному ТВ — только сеть, без каста.** После зелёного чек-листа:
   стенду добавляется нога в `192.168.100.0/24` (vmbr100/VXLAN, как у CT101),
   в конфиг прописывается адрес ТВ `192.168.100.102`. На этом моя часть заканчивается:
   **тест на живом ТВ владелец делает сам.** Я ТВ не кастую и не бужу.

После приёмки владельцем на ТВ — отдельным решением: прод-LXC (VMID 1xx) как код
в gitops, доки в vault (`infra/Services/torrcast.md`), судьба алиаса `cast` на agent-ops.

## 8. Справочник окружения (всё, что нужно исполнителю)

| Что | Где | Примечания |
|---|---|---|
| Разработка | agent-ops `192.168.1.50` | dev-среда; ⚠️ egress через VPN (NL) — доступность трекеров с неё НЕ показатель, мерить со стенда/caffeine |
| Стенд | новый LXC на caffeine, создаётся с `root@192.168.1.15` (`pct`) | VMID из тестового диапазона **500–1000**; Debian 12, unprivileged; 4 vCPU, 8 ГиБ RAM (из них ~4 ГиБ RAM-кэш TorrServer); сеть `vmbr0`, **статический** IP из LAN (DHCP-шлюз недоступен для резерваций — выбрать свободный, зафиксировать в доках) |
| Git-репа | домашний GitLab `192.168.1.25`, `git@192.168.1.25:…/torrcast` | коммиты `anysda <anysdaw@gmail.com>`; деплой на стенд ТОЛЬКО из репы |
| TorrServer | на стенде, `:8090` | API: `POST /torrents` (add magnet), поток `GET /stream?link=<hash>&index=<n>&play`; кэш в RAM |
| Prowlarr | на стенде, `:9696` | Torznab: `GET /api/v2.0/indexers/all/results?apikey=<локальный>&query=…`; apikey генерит install.sh |
| Split-DNS | AdGuard CT301 `192.168.1.57` | DNS-rewrite `torrcast.anysda.space` → IP стенда; :53 не трогать, только rewrite |
| Обход DPI для индексеров | zapret CT302 `192.168.1.58` | если RuTor/зеркала биты DPI — маршрутить запросы индексеров через него, НЕ через облачный VPN |
| Образец LE-контура | amnezia, `cast-hls.service`, серт `cast.anysda.space` | посмотреть механику выпуска для §3 (https на LAN-хосте); на amnezia НИЧЕГО не менять |
| ТВ (финал, не трогать) | `192.168.100.102`, сеть `192.168.100.0/24` = vmbr100/VXLAN | нога добавляется стенду ТОЛЬКО на этапе 6; Chromecast встроен в Samsung Q70D; порт 8009 открыт даже в standby — **любой каст/коннект будит ТВ, поэтому до этапа 6 к этому адресу не прикасаться** |
| Полные доки | vault `~/homelab/infra/` (`Network.md`, `Services/`) | изменения инфры (DNS-rewrite, новый LXC) документировать там же в той же ходке |

## 9. Правило сдачи (Definition of Done)

**Работа, свалившаяся в блокер, не принимается.** Исполнитель имеет право
остановиться ровно в двух состояниях:

1. **«Готово»**: чек-лист §7.5 полностью зелёный, И составлена справка
   **«почему это точно заработает на телевизоре»** — по каждому пункту реестра
   ТВ-рисков ниже написано, чем он закрыт и как это проверено на mock. Формулировка
   «на mock работает, на ТВ посмотрим» — НЕ сдача.
2. **Явный вопрос владельцу**: конкретный, с вариантами и рекомендацией
   (например, «RuTor недоступен всеми N способами: пробовал прямые зеркала,
   zapret, onion — нужна учётка RuTracker, дай или скажи копать дальше»).

«Не получилось — остановился» между этими состояниями = брак. Уперся —
ищи обход: другое зеркало, другой индексер, другой инструмент, другой способ
добыть серт. Инструмента не хватает — поставь нормальный, а не костыляй.

**Реестр ТВ-рисков** (различия mock ↔ реальный Chromecast/Q70D), каждый пункт
обязан быть закрыт в справке:

| Риск | Чем закрывается |
|---|---|
| HEVC не декодируется ресивером | дефолт — H.264-релиз; HEVC только с пометкой ⚠ |
| Битрейт-потолок декодера (~20 Мбит/с) | пик битрейта релиза меряется, жирные помечаются |
| AC3/DTS не играет | аудио всегда перекодируется в AAC stereo — passthrough запрещён §3 |
| Chromecast требует доверенный HTTPS | реальный LE-серт, проверен снаружи стенда, не self-signed |
| Chromecast молча не играет без CORS | `Access-Control-Allow-Origin: *`, mock валидирует заголовки |
| Приёмник не умеет наш HLS-диалект | формат зафиксирован §3 (TS ~4 с, один вариант) — ровно тот, что kinocast годами скармливает этому же ТВ |
| ТВ просыпается от каста | до этапа 6 адрес ТВ в конфиге отсутствует физически |

## 10. Открытые вопросы

1. **Доступ к индексерам из LAN:** RuTor и зеркала могут быть за DPI-блоком —
   решаем на этапе 1 (zapret-контур в LAN уже есть, вероятно хватит его).
   Внешних регистраций это не добавляет.
2. **Учётки RuTracker/Kinozal** — не требование, а опциональное обогащение после
   MVP, если владелец захочет более жирную выдачу.
