# torrcast v2 — ТЗ по итогам живого теста

Продолжение `SPEC.md` (v1). v1 закрыт: код, стенд CT501 (`192.168.1.62`), приёмка на mock.
Живой тест на Samsung Q70D показал: **работает не то, что проверялось**. v2 — про то, чтобы
владелец включил фильм и посмотрел его целиком, ни разу не задумавшись о торрентах.

Всё, что здесь написано, опирается на факты из `docs/diag-tv.md` и `docs/diag-tty.md`
(диагностики 05-08-2026), а не на предположения.

---

## 0. Шесть претензий владельца — и что с ними

| # | Претензия (дословно) | Диагноз | Статус |
|---|---|---|---|
| 1 | «в итоге так и не заработало» | `_hold` снимал позицию сразу после LOAD; приёмник закономерно 0.7–1.5 с в `IDLE` → `playing=False` → юнит гасился через 1 с после «▶ → ТВ» | ✅ причина устранена в ветке `fix/tv-live-load`, 12 мин живого показа |
| 2 | «не хочу видеть какие там файлы, хочу выбрать фильм и озвучку» | таблица релизов и список файлов — обязательный шаг v1 | § 2 |
| 3 | «консоль не поддерживает кириллицу» | ssh даёт pty с **IUTF8 off**: забой стирает 1 байт, эхо — 1 колонку; русская буква = 2 байта | § 3 |
| 4 | «прогноз по времени запуска очень хуёвый» | обещали 15–30 с; по факту 24 с холодный, но 9 с из них — самоповреждение `readrate=1`, а прогрева под меню в коде **нет вовсе** | § 4 |
| 5 | «попробовать каст по HTTP» | проверено на живом ТВ: HTTP играет идентично HTTPS (LOAD→PLAYING 0.66 с против 0.75 с) | § 5 |
| 6 | «можно гонять тесты на реальном ТВ» | ограничение v1 снято | § 7 |

---

## 1. Цель v2 и единственный критерий успеха

**Владелец говорит `cast моана 2`, жмёт Enter один-два раза и досматривает фильм до конца
без единого касания консоли.** Всё остальное — детали реализации.

Приёмка v2 идёт **только на реальном Q70D**. Mock остаётся исключительно как регрессионный
прогон в тестах; ссылаться на него как на доказательство работоспособности запрещено.

**Поправка владельца 05-08 (вечер):** длинные прогоны на ТВ (20+ минут, полный фильм, серия
целиком) агенты НЕ гоняют — их проводит владелец сам при реальном просмотре. Агентам на ТВ
разрешены только короткие смоки: старт → картинка → минута-другая → stop. Пункты приёмки,
требующие длинного показа, остаются в чек-листе, но закрывает их владелец.

**Дефекты, пойманные владельцем на живом прогоне 05-08 (чинить обязательно):**
1. `cast moana` → «Moana (2016)» → «Дорожки: читаю поток…» — **зависло насмерть**, без
   прогресса, таймаута и перехода к следующему релизу. Любая фаза обязана иметь живой
   прогресс, таймаут и честный fallback.
2. На рабочем пути «Дорожки: читаю поток…» молчало ~20 с (метаданные 3.2 с + ffprobe 17.1 с)
   уже ПОСЛЕ ответов на вопросы — ровно то, что §4 велит уносить в фоновый прогрев под меню.
3. «Moana 2 (2024)» — полнометражка — определена как **сериал**, с бессмысленной строкой
   «Серии: серий 1: s1e1…s1e1». Фильм не должен превращаться в сериал из-за цифры в названии;
   строки про серии в выводе фильма недопустимы. (§8 запрещает переписывать парсер целиком —
   точечная починка классификации сюда не относится.)

## 1.1 Как ведётся работа (модель исполнения) — ОБЯЗАТЕЛЬНО

Наследуется из §6.1 v1 и подтверждено практикой v1 — поэтому вынесено в v2 явно, а не молчанием.

**Оркестратор руками не кодит, не дебажит и не ковыряет стенд.** Его контекст — контекст
главного разработчика проекта, и он обязан дожить до конца работ. Кодинг, отладка, прогоны на
ТВ, развёртывание, снятие корпусов — всё это делают субагенты. Оркестратору остаётся:
декомпозиция, постановка задач, ревью результатов, ведение чек-листов, решения по спорным
развилкам и общение с владельцем.

Причина дословно та же, что в v1: сохранность контекста главной сессии. Если оркестратор
начнёт отлаживать сам, сессия закончится посреди проекта и работа осиротеет.

**Субагенту ставится цель и ограждения, а не рецепт.** Он сам выбирает путь, сам дебажит свои
ошибки и возвращает результат, а не лог мучений. Это не стилистическое предпочтение — так
найдено практически всё важное в v1, причём именно там, где исполнителю дали свободу копать:

- показ гас через секунду после LOAD, потому что закономерный `IDLE` приёмника считался отказом —
  нашлось только на живом ТВ, при разрешении «воспроизведи и смотри с обеих сторон»;
- `2x2` в имени файла оказалось названием телеканала, а не серией — поймано на живой раздаче,
  спасла проверка связности номеров по всему списку файлов;
- кириллица ломалась из-за pty с выключенным `IUTF8` (забой стирает байт, эхо — колонку) —
  найдено после того, как первая, ошибочная гипотеза про кодировки была честно отброшена.

Отсюда же практическое следствие для постановки задач: **требовать доказательство, а не отчёт
о работе** — цифры, логи, воспроизведение. И принимать честное «не сошлось» как результат:
первый прогон «Моаны» не сошёлся и вскрыл два дефекта, которые на ТВ выглядели бы как молчаливая
потеря середины фильма.

Параллельные исполнители разводятся по живым объектам (стенд / репа / ингресс / ТВ), а не по
файлам; в одной репе — через git worktree.

## 2. UX: фильм и озвучка, больше ничего

Из обязательного пути уходят таблица релизов и список файлов. Остаётся:

```
$ cast тачки
«Тачки» — найдено:
  1. Тачки (2006)
  2. Тачки 2 (2011)
  3. Тачки 3 (2017)
Что смотрим? [1]: 1

Озвучка: 1. Дубляж  2. Многоголосый  3. English
Озвучка? [1]:

▶ «Тачки» (2006) · 1080p · дубляж → ТВ     [идёт подготовка: … ]
```

- **Релиз выбирается сам** по правилам v1 (самый обсиженный кандидат: H.264 либо HD-источник;
  битрейт > 20 Мбит/с исключён; настоящий кодек подтверждается `ffprobe`). Если выбранный
  релиз не подошёл — строка «релиз не годится (hevc) — беру следующий» и переход дальше;
  молчаливых подмен нет (§1 v1 в силе).
- **Сериалы: список серий не показывается.** `cast <сериал>` играет следующую невиденную серию.
- Осмотреть внутренности можно только явно: `--release N`, `--file N`, `cast releases <запрос>`
  (печатает старую таблицу и выходит). Это отладочные ручки, не часть счастливого пути.
- Меню франшизы и озвучки пропускаются, когда выбор единственный.
- Вопрос «Продолжить? [Да/сначала]» (§2.3 v1) остаётся — он про намерение, а не про технику.

**Требование к вопросам:** любой вопрос принимает и цифру, и Enter. Русский ввод в вопросах
допустим, но никогда не обязателен — «Да/нет» отвечается и пустым Enter'ом.

**Правка владельца 06-08 (озвучка, отменяет меню озвучки выше):** ✅ сделано 06-08.
- Меню озвучки в счастливом пути НЕТ. По умолчанию берётся «самая нормальная» сама (порядок
  здравого смысла: русский дубляж → русский многоголосый → прочий русский → оригинал; выбор
  печатается в строке запуска — молчаливых подмен нет).
- Выбор озвучки доступен только флагом (меню по флагу — как старые `--release N`/`cast releases`).
- Выбранная озвучка ЗАПОМИНАЕТСЯ отдельно для каждого фильма/сериала (как в kinocast):
  `cast сопрано` играет той озвучкой, которую владелец выбрал для «Сопрано» в прошлый раз;
  явный флаг переопределяет и перезаписывает память.

### 2.0 Озвучка: как сделано (06-08-2026)

**Лестница дефолта** (`AudioTrack.step`, `Media.default_track`): русский дубляж → русский
многоголосый/закадровый → русский двухголосый → русский авторский/одноголосый → прочий
русский → оригинал → чужой дубляж; тифлокомментарий и комментарии съёмочной группы — ниже
всех. Внутри ступени порядок авторский, то есть тот, в котором дорожки лежат в файле:
сборщик кладёт первой ту озвучку, которую сам считает основной.

Правила писаны по живым дорожкам стенда (`scripts/voicedump.py`, шесть запросов
06-08-2026), а не из головы, и три из них так и не угадывались:

| раздача | дорожки | что решает |
|---|---|---|
| «Тачки 3» WEB-DL | `rus·Дубляж`, `rus·Дубляж для слабовидящих`, `ukr·Дубляж`, `kaz·Дубляж`, `eng·Оригинал` | тег языка сильнее заголовка (у чужих дубляжей то же русское слово), а тифлокомментарий — служебная дорожка |
| «Моана 2» BDRip | три `rus·Дубляж. (…)`, пять `rus·MVO (…)`, `eng·Original` | дубляж выше MVO; из трёх дубляжей — первый |
| «Интерстеллар» IMAX | `rus·DUB-Blu-ray CEE`, `ukr·DUB-…`, `rus·MVO-студия «Омикрон»`, `rus·AVO-Сербин`, `rus·AVO-Живов`, `rus·VO-Есарев`, `eng·Original` | маркеры пишут слитно с текстом через дефис |
| «Киберпанк» s1e1 | `rus·[TVShows][MVO]`, `jpn` без заголовка | дубляжа нет — спускаемся на ступень; оригинал без подписи не выигрывает |
| «Матрица» HDTVRip | `rus·DUB (Rus) / AC3 / 6 ch / 384 kbps / 48 kHz`, `rus·AVO Визгунов (Rus) / …`, `eng·Original (Eng) / …` | технический хвост режется: подпись — она же ключ памяти |

**Флаг** — `--voice N` (взять дорожку N), `--voice` без номера (то самое меню, убранное со
счастливого пути), `cast voices <запрос>` (список с пометками «дефолт» и «запомнено»).
Прежнее имя `--audio N` живо как синоним. Номера, которого нет, — честный отказ со ссылкой
на `cast voices`.

**Память** — поле `voice` записи состояния (`Entry.voice`), то есть **на картину**: у
сериала запись одна на всю раздачу, поэтому память общая на сериал, а не на серию, и
переезжает на следующую серию вместе с релизом (`Entry.advance`). Хранится **подпись**
дорожки (`rus · MVO (LostFilm)`), а не номер: в следующий раз верх отбора может оказаться
другим релизом, где та же озвучка стоит под другим номером. Пишет память только явный
выбор; автовыбор её не трогает, а если запомненной озвучки в релизе нет — говорим вслух
(«озвучки «…» в этом релизе нет — беру обычную») и **память не стираем**: релиз временный,
выбор владельца — нет.

Живая проверка на стенде (`--dry`, приёмник по умолчанию не поднимался, к ТВ ни одного
пакета; состояние владельца читалось, писалось в `TORRCAST_STATE=/root/voice-live.json`):

| запрос | было | стало |
|---|---|---|
| `моана 2 --dry` | `Озвучка: 1. rus · Дубляж. (MovieDalen)  2. … 9. eng · Original` + вопрос `Озвучка? [1]:` | вопроса нет, `(--dry) «Моана 2» (2024) · 720p · rus · Дубляж. (MovieDalen)` |
| `киберпанк --dry` | два вопроса: франшиза и `Озвучка? [1]:` | один вопрос (франшиза), `s1e1 · 1080p · rus · [TVShows][MVO]` |
| `моана 2 --dry --voice 6` | — | `· rus · MVO (LostFilm)` |
| `моана 2 --dry` с памятью `rus · MVO (LostFilm)` | — | `· rus · MVO (LostFilm)`, ни вопроса, ни флага |
| то же с памятью `rus · Кубик в кубе` | — | `озвучки «rus · Кубик в кубе» в этом релизе нет — беру обычную`, память цела |
| `киберпанк --dry` (сериал, память в записи) | — | `s1e3 · 1080p · rus · [TVShows][MVO]` |

⚠️ **Цена флага на пути продолжения.** У сериала и у «Продолжить?» дорожек никто не читал —
показ идёт по записи состояния. Поэтому `--voice` там перечитывает раздачу (`_revoice`):
метаданные плюс ffprobe, на стенде 22–31 с на холодной раздаче. Счастливый путь этой цены
не видит, но и прятать её незачем — фаза «дорожки…» с бегущим временем видна.

🔴 **Грабля, найденная живым прогоном, а не тестом:** первая версия звала `_revoice` до
проверки «а есть ли что продолжать», и флаг поднимал в TorrServer раздачу записи, которую
никто играть не собирался (падало на её магните: `TorrServer не отвечает … 400`). Читать
дорожки записи можно только тогда, когда по этой записи и пойдёт показ (`_voiced`); тест
на это лежит в `tests/test_voices.py`.

**Правка владельца 06-08 (динамический битрейт — §6.2):** ✅ сделано 06-08 вечером, см.
§6.2 — там же 🔴 про то, что потолок 16 Мбит/с оказался завышен. Цель — «всегда и везде 1080p».
Упаковщик заранее видит тяжёлые куски (карта кадров даёт байты/секунды по сегментам) и, пока
идёт остальной фильм, перекодирует ТОЛЬКО их. Требования жёсткие: кратковременное снижение
качества допустимо; подгрузы (ребуферы) в процессе НЕ допустимы; подгрузы на стыке смены
качества НЕ допустимы (бесшовно). Следствие: потолок битрейта перестаёт быть фильтром выбора
релиза — тяжёлый честный 1080p становится игр/абельным через выборочное перекодирование.
(§8 «видео не перекодируем» в этой части отменяется владельцем — но перекодирование только
выборочное, тяжёлых кусков, не всего фильма.)

## 2.1 Экран ТВ: прогресс, время, пульт — ОБЯЗАТЕЛЬНО

Требование владельца 05-08-2026 (по опыту kinocast): **на ТВ всегда видны общая длительность
фильма и текущая позиция** (таймлайн), и **пультом можно мотать в любую точку фильма** — не
только в пределах окна упаковки.

- Причина текущего брака: kinocast отдаёт приёмнику VOD-плейлист с известной длительностью,
  torrcast — скользящий live-плейлист; для приёмника это «эфир», у которого нет ни начала,
  ни конца, ни таймлайна.
- Как чинить — решает исполнитель (VOD/EVENT-манифест на всю длительность с упаковкой по
  требованию, длительность в метаданных LOAD, иное), но критерий один: приёмник знает
  длительность (`duration` в MEDIA_STATUS = длине фильма), позиция честная, seek пультом
  в любую точку работает и приводит к продолжению показа с этого места.
- Ограничение железа: целый фильм в tmpfs/на диск стенда не влезает — «просто не чистить
  сегменты» решением не является.
- В приёмку §7E добавляется пункт: таймлайн и общее время видны, пульт мотает в любую точку.

## 3. Консоль: ввод обязан работать из коробки

- При старте включать **IUTF8** на stdin и восстанавливать режим на выходе (проверено:
  три строки, `termios`, контекстный менеджер; при отсутствии tty — no-op).
- Любой ответ пользователя проходит **чистку висячих суррогатов** — битый ввод не должен
  доезжать до парсера ни при каких обстоятельствах.
- `input()` без tty **не висит**: берёт дефолт и продолжает (сейчас — вечное ожидание,
  наблюдалось 180 с).
- Убрать из вывода `→ ⚠ ▶ ≥` — заменить словами («внимание», «играю»). Информации не несут,
  ломаются в части терминалов. Кириллица и `« » — …` остаются.
- `cast doctor` — одна команда самопроверки: терминал, локаль, доступность Prowlarr/TorrServer,
  адрес ТВ, доступность ТВ по :8009, свежесть серта (если HTTPS). Печатает вердикт по-русски.

## 4. Время старта: честный прогресс вместо обещаний

Обещание «15–30 с» из §3.1 v1 **отменяется**. Вместо него:

- **Живой прогресс по фазам** прямо в консоли, с бегущим временем:
  `поиск… → метаданные (DHT)… → дорожки… → упаковка… → жду телевизор…`.
  Пользователь всегда видит, на чём стоим, и не гадает, повисло или нет.
- «Старт NN с», который печатает CLI, обязан означать **картинку на экране**, а не «упаковка
  пошла». Сейчас это разные вещи.
- Сокращение реального времени — три источника, все измерены:
  1. `readrate=1` стоил 9 с на первом сегменте — убрано, дефолт пересматривается (см. §6).
  2. **Прогрев под меню, обещанный в §3.1 v1, в коде отсутствует** (`cli.py:505` ждёт результат
     тут же, уже после выбора). Сделать по-настоящему: топ-2–3 релиза уходят в TorrServer
     фоном, пока человек отвечает на вопросы.
  3. Проверка кодека стоит лишнего цикла метаданных — совмещать с прогревом.
- Ориентир после правок: **холодный старт ≤ 15 с на тёплом рое, resume ≤ 5 с**. Цифры
  подтверждаются замерами на живом ТВ, а не расчётом. Если не достигается — честно пишем
  в отчёте достигнутое, а не подгоняем формулировку.

## 5. Транспорт: HTTP, и вся система — одна машина дома

Проверено на живом Q70D: HTTP и HTTPS играют одинаково (LOAD→PLAYING 0.66 с против 0.75 с).
Это не «упрощение по вкусу», а **устранение целого класса зависимостей** — прямое требование
владельца: «ровно из-за этого я не любил кинокаст».

**Целевое состояние: torrcast целиком живёт на одной домашней ВМ.** Ни облака в пути данных,
ни сертификатов, ни внешних имён, ни проброса портов. Поток: стенд → телевизор, и всё.

Что уходит из системы:

| Зависимость | Зачем была | Решение |
|---|---|---|
| LE-серт `torrcast.anysda.space` | «Chromecast требует доверенный HTTPS» | не нужен — HTTP проверен на ТВ |
| Traefik на home-ingress (VPS) | выпускал и продлевал этот серт | домен убрать из ACME-резолвера |
| split-DNS rewrite в AdGuard | имя → LAN-адрес стенда | не нужен, URL по голому IP |
| доставка серта таймером + ssh-ключ на ингрессе | обновление серта на стенде | снять целиком |
| самоподписанный CA (для шима) | доверие Prowlarr к шиму | пересмотреть вместе с §8 |

Требования:

- **Дефолт — HTTP**, `hls_base_url` строится на **IP-адресе, а не на имени**. DNS в пути
  воспроизведения не участвует вовсе: упадёт AdGuard — каст продолжает работать.
- **Адрес брать со стороны телевизора**: у стенда есть нога в сегменте ТВ (`192.168.100.62`),
  поэтому URL вида `http://192.168.100.62:8080/…` даёт прямой путь в одном L2-сегменте.
  Сегодня поток идёт лишним хопом через SNAT VyOS (ТВ видит нас как `192.168.1.55`).
  ⚠️ Прямой путь логичен, но **на ТВ не проверялся** — проверить и подтвердить замером;
  не сработает — откатиться на LAN-адрес `192.168.1.62` и записать причину.
- HTTPS остаётся **выключенной опцией** (`transport: https`): код написан и работает,
  выбрасывать его незачем. Но в обязательном пути его нет, и приёмка идёт по HTTP.
- CORS `Access-Control-Allow-Origin: *` остаётся обязательным на всех ответах.
- **Чистка ранее сделанного** (отдельным шагом, аккуратно, как код):
  домен `torrcast.anysda.space` убрать из ACME-резолвера Traefik на home-ingress и снести
  роутер `acme-only-1` на loopback-entryPoint; снять rewrite в AdGuard; снять таймер доставки
  и forced-command ключ на ингрессе. ⚠️ Ингресс — прод с SSO: правка минимальная, через
  gitops/cloud, с проверкой живости чужих доменов после применения (в роли уже есть страховки
  на случай битой статики — они появились после падения 04-08).
- Единственная внешняя зависимость, которая остаётся, — **поиск в интернете** (индексеры).
  Это неустранимо: каталог живёт снаружи. Но данные фильма идут только из торрент-роя,
  посредников в пути потока нет.

## 6. Устойчивость показа — главный незакрытый риск

Диагностика поймала воспроизводимое зависание: когда `Packer.pace()` надолго останавливает
ffmpeg сигналом и манифест перестаёт меняться, **приёмник намертво виснет в BUFFERING** —
держит простаивающий коннект и не запрашивает ничего (0 пакетов за 25 с) при живых сегментах
на диске. При непрерывной упаковке 12 минут прошли без сбоев.

Требуется:

- **Полный фильм от начала до конца на реальном ТВ, без единого зависания.** Это главный
  пункт приёмки v2. 12 минут — не доказательство.
- Убрать сам класс проблемы, а не симптом: гипотеза — `-readrate_initial_burst` (ffmpeg ≥ 6.1):
  burst первых ~30 с, дальше чуть быстрее реального времени, и останавливать ffmpeg сигналом
  не нужно вовсе. Проверить; если не сработает — искать другой механизм, но SIGSTOP-пауза
  под работающим приёмником должна исчезнуть.
- **Сторож приёмника** (рецептура kinocast, годами работающая на этом же ТВ): опрос раз в 2 с
  с `update_status`, `IDLE` до первого проигрывания = «ещё грузится» (терпим до ~90 с),
  `ERROR` → повтор LOAD (до 2 раз), неподвижный BUFFERING → нудж `seek` **только вперёд**
  (первая версия давала бесконечную лесенку назад 2:12 → 1:58 → 1:44 — поймано живьём).
- **Перемотка назад глубже окна** сейчас упирается в 404: манифест перечисляет уже вычищенные
  сегменты. Решить честно — либо окно достаточной глубины, либо рестарт ffmpeg с `-ss`
  (§3 v1), либо манифест не врёт о том, чего нет.
- Тест длинного показа обязан проверять и **память/tmpfs** (наблюдалось 70–148 МБ) — рост без
  предела недопустим.

### 6.0 Нарезка: что сделано 06-08-2026 (детерминированность и бисект)

**Детерминированная нарезка сделана.** Границы сегментов теперь абсолютные: сегмент `vN`
занимает одно и то же место фильма при любом старте упаковки и любой перемотке.

- Механизм: `-f segment -segment_times <абсолютный список> -copyts` вместо `-f hls
  -hls_time`. Сегментный муксер принимает **список** мест реза, а не шаг, — это
  единственный способ положить границы туда, где они стоят в манифесте.
- ⚠️ Ключевая грабля: муксер считает эти времена **от первого пакета прогона**, а `-ss`
  уводит ffmpeg на опорный кадр не позже запрошенного места и **не обязательно на
  ближайший** (замер: `-ss 66.150` на «Моане» 2016 даёт первый кадр 62.688 — через один).
  Вычислить это нельзя, поэтому место старта измеряется пробным прогоном в один кадр
  (`pack_start`, 0.5–1.7 с, пара мегабайт), и список границ считается от него.
- Докатка от опорного кадра до границы уходит в отдельный сегмент с номером `slot-1` в
  каталоге прогона и наружу не выкладывается: под этим именем уже может лежать честный
  кусок прошлого прогона. Наружу сегмент попадает переименованием, когда ffmpeg открыл
  следующий, — «файл есть» снова значит «кусок готов».
- Границы стоят **на опорных кадрах** (карта из индекса Cues mkv, `torrcast/mkv.py`):
  каждый сегмент декодируется сам по себе, `EXTINF` — фактическая длина куска,
  в манифесте честный `EXT-X-INDEPENDENT-SEGMENTS`. Нет карты (не mkv, нет Cues) — ровная
  сетка по `hls_segment`, как было.
- Доказательство (`scripts/gridcheck.py`, «Моана» 2016, три прогона — со слотов 0, 5 и 7):
  байты видео (md5 битстрима H.264) для v7…v12 **совпали во всех трёх**, расхождение
  границы с манифестом 0.000 с. На живом показе то же число видно в `TORRCAST_TRACE`:
  0.000 с при упаковке из середины и 0.042 с (ровно кадр) при упаковке от нуля — ffmpeg
  не пускает dts ниже нуля и сдвигает весь фильм на кадр.

**Бисект убийственной границы: правило найдено, и оно не про границу.** Плохая
конфигурация стала повторяемой (3 из 3 прогонов) и разобрана по одному изменению за раз:

| прогон | сетка | старт показа | итог |
|---|---|---|---|
| 1 | ровно 4 с, фаза от нуля | с 76 с | чисто до 104 с |
| 2, 6 | ровно 4 с, фаза от нуля | с 0 | **встал на 84.16, 6.5–7 с** |
| 3 | 4 с, граница 84.0 убрана (кусок 80…88) | с 0 | **встал на 80.15, 8.6 с** |
| 5 | ровно 4 с | с 200 с, 100 с показа | чисто (лёгкое место фильма) |
| 8 | по опорным кадрам, шаг 4 с | с 0 | **встал на 83.18, 8.6 с** |
| 7 | по опорным кадрам, шаг 10 с | с 0 | чисто до 94 с |

Исключено замерами: конкретная граница (уберёшь 84.0 — встаёт на 80.0), совпадение
границы с опорным кадром (сетка по кадрам с шагом 4 с встаёт так же), разрез тяжёлого GOP,
время от начала показа и число проигранных сегментов (100 с и 25 сегментов в лёгком месте —
чисто), доставка (сегменты уезжают за 0.1 с, приёмник получил их за 20 с до подвиса) и
голодание по упаковке (запас 55–67 с в момент подвиса).

Остаётся ровно одно: **длина сегмента против битрейта места**. Подвисает установившийся
показ (буфер приёмника уже полон) в самом тяжёлом месте фильма — 66…88 с «Моаны» 2016, два
самых тяжёлых GOP фильма подряд, 12–13 Мбит/с. Сегменты 4.0–5.0 с (6–8 МБ) там убивают
показ, 8 с — тоже, 10.0 и 14.6 с (15.7 и 21.9 МБ) — нет. То есть дело не в байтах (провальный
кусок легче прошедшего), а в **секундах**: короткий сегмент даёт приёмнику мелкий буфер, и
на пике битрейта он срывается в BUFFERING, после чего перекачивает уже скачанные куски
(видно в трассе: v17…v19 запрошены дважды).

**Правило, заложенное в нарезку:** сегмент не короче 10 с (`hls_segment`), а сетка по
опорным кадрам делает его ещё длиннее ровно там, где GOP длинный. Фаза, из-за которой
казалось, что виновата граница, была артефактом: она менялась вместе с местом старта
показа, а решает — установившийся показ он или ещё набирает буфер.

### 6.1 Факты с живого Q70D (дописано 05-08 вечером, по замерам)

- Виснущий BUFFERING — дефект **нарезки HLS**, не темпа/роя/подачи: тот же битстрим
  прогрессивным mp4 играет чисто. При этом простые правила не подтвердились: «тяжёлый GOP
  разрезан» и «сегмент без опорного кадра» приёмник НЕ роняют (проверено на злейшем GOP
  «Моаны 2» 13.31 МБ/10.38 с). Воспроизводимая зависимость — от **фазы сетки**: сетка 4 с
  с нуля встаёт ровно на 84.0 с, та же сетка со старта 1:10 проходит чисто. Правило,
  предсказывающее убийственную границу, пока не найдено.
  ⚠️ Поправка 06-08 (§6.0): «фаза» оказалась артефактом — со старта 1:10 менялась не она,
  а то, что показ ещё набирал буфер. Решает длина сегмента, а не место границы.
- `split_by_time` считает границы от первого пакета прогона, а `-ss` уводит на опорный кадр
  раньше — **имя сегмента врёт о содержимом до ~10 с**, и фаза сетки меняется после каждой
  перемотки. Требование: границы сегментов обязаны быть **абсолютными и детерминированными**
  независимо от места старта упаковки. ✅ Сделано 06-08, см. §6.0.
- 🔴 Грабля процесса: `install.sh` молча НЕ обновлял код в venv (та же версия → pip «already
  satisfied»; `--force-reinstall` без `--no-cache-dir` ставил старое колесо из кэша).
  Починено; урок — после выкатки сверять код в venv с репой, результат «после выкатки»
  без такой сверки не засчитывается.

### 6.2 Динамический битрейт: тяжёлые куски перекодируются заранее (06-08-2026, вечер)

Правка владельца — «лучше чтобы всегда и везде было 1080»: тяжесть релиза лечится не
отбором, а выборочным перекодированием, бесшовно и без единого подгруза. Сделано; ниже —
что замерено, что работает и что осталось.

**Замер 1. Скорость кодека на стенде** (`scripts/recodebench.py --speed`, CT501, 4 vCPU
E5-2696 v4, аппаратного кодировщика нет — в LXC нет `/dev/dri`). Вход — четыре настоящих
сегмента «Моаны 2» 13.3 ГБ, 57.6 с, 23.7 Мбит/с, 1920×960 H.264 High@L4.1.

| пресет | wall, с | ×реального времени | выход, Мбит/с |
|---|---|---|---|
| ultrafast | 13.2 | **4.36×** | 12.4 |
| superfast | 22.0 | **2.62×** | 12.4 |
| veryfast | 37.5 | **1.54×** | 12.1 |
| faster | 55.4 | 1.04× | 12.1 |
| fast | 79.8 | 0.72× | 12.2 |
| medium | 105.7 | 0.55× | 12.2 |

⚠️ `-threads` тут почти ничего не решает: x264 держит 300–390 % процессора при любом
значении (свои потоки на lookahead). Ограничивать кодировщик приходится приоритетом
(`nice 15`), а не числом потоков.

**Замер 2. Профиль тяжести «Моаны 2» 13.3 ГБ** (`--profile`). Считается из уже снятой
карты опорных кадров, стоит ноль запросов к рою: смещения кадров дают байты и секунды
каждого сегмента сетки до всякой упаковки.

⚠️ **Байты карты — это контейнер целиком, а на ТВ уезжает видео и одна дорожка.** У этого
релиза десять звуковых дорожек и восемь субтитров: контейнер идёт 19.2 Мбит/с, а на ТВ
уезжает 15.0. Поправка постоянна — замер на восьми упакованных сегментах подряд дал
3.97…4.26 Мбит/с при среднем 4.10, — поэтому она вычитается и уточняется по факту первых
же выложенных сегментов (`Weights.calibrate`, на прогоне сходится к 4.2 за 8–10 замеров).
Проверка модели против настоящей упаковки: предсказание расходится с фактом на 0.5–0.8
Мбит/с (v369 12.4 против 13.0, v372 14.5 против 15.2, v376 17.3 против 18.1).

Фильм 99.6 мин, 447 сегментов, средний доставляемый 15.0 Мбит/с, пик 31.5. Тяжёлого:
≥12 — 67 % фильма, ≥15 — 46 %, ≥18 — 29 %, ≥20 — 16 %. Самая длинная серия подряд идущих
тяжёлых при пороге 10 — 65 сегментов.

**Замер 3. Модель показа** (`--plan`): кодировщик работает с нулевой секунды, по порядку
фильма, горизонт 300 с. При 1.40× (veryfast) и пороге 10 Мбит/с опаздывают **2 сегмента
из 335**, худшее опоздание 14 с, работы кодировщику 55.6 мин на 100-минутный фильм. При
2.35× (superfast) — 1 из 335. Неизбежно опаздывает только `v0`: он тяжёлый с нулевой
секунды, и форы до него не набирается ни при какой скорости.

**Как сделано** (`torrcast/recode.py`, 🔴 нового демона нет — поток внутри юнита показа):

* профиль тяжести строится из карты при старте показа, тяжёлые слоты известны сразу;
* кодировщик заходами по несколько подряд идущих тяжёлых кусков гонит **тот же**
  `ffmpeg_pack_command` с той же сеткой, теми же `-copyts`, тем же звуком — меняется
  ровно `-c:v copy` на libx264 с капом. Готовое ложится в `<показ>/recode/`;
* наружу перекод выкладывает **не кодировщик, а упаковщик**: `Packer.publish` при выборе
  между копией и перекодом берёт перекод. Выкладка осталась ровно в одном месте кода, и
  инвариант «край двигает только состоявшееся переименование» (§7.4) не размылся;
* очередь — от места показа вперёд, перемотка переприоритезирует её на следующем заходе;
* пресет выбирается по сроку: успевает veryfast — берём его, не успевает — superfast,
  ultrafast. Отдельной «реалтайм-ветки» не понадобилось: это она и есть.

**⚠️ Две грабли, каждая стоила прогона.**

1. При `-c:v copy` ffmpeg по `-ss` встаёт на опорный кадр **раньше** запрошенного и
   докатывает до границы (§6.0), а при перекодировании тот же `-ss` **точен** — лишние
   кадры декодируются и выбрасываются. То есть у перекодирующего прогона докатки нет
   вовсе и `at` равен границе сетки ровно. Первая версия честно мерила `at` пробным
   прогоном, как для копии, — и весь прогон уезжал ровно на сегмент: `v359` содержал
   место `v360`.
2. `-force_key_frames` сравнивает время кадра с запрошенным как есть, а граница печатается
   с тремя знаками. Округление вверх (4909.9167 → «4909.917») уводило опорный кадр на
   следующий, и на стыке копии с перекодом терялся один кадр. Опорные кадры теперь
   просятся на `SPLIT_SLACK` раньше границы — тот же допуск, с которым режет муксер.

**⚠️ Третья грабля, найденная живым ТВ: упаковщик обгонял кодировщика.** На старте прогона
`-readrate_initial_burst` выкладывает 60 с разом, и тяжёлые куски уходили копией просто
потому, что ffmpeg кодировщика ещё не стартовал. Лечится придержанием: копия тяжёлого
куска ждёт в каталоге прогона, пока перекод не готов, — но **только если перекод успеет
раньше, чем показ дойдёт до этого места** (`Recoder.holding`). Плоский порог «держим всё,
что дальше 25 с» не работал в обе стороны: `v359` (26 Мбит/с) не придержали, хотя
кодировщику было нужно три секунды, а `v360` придержали и отпустили раньше, чем заход до
него дошёл. Кусок, до которого показ уже дошёл, не держим никогда: ожидание под носом у
показа — это и есть подгруз.

**Доказательство бесшовности (локально, `Feed` + `Recoder` + настоящий ffmpeg).** 17
сегментов подряд через тяжёлый участок 4884…5131 с, из них 13 перекодированных: у каждого
перекодированного первый кадр опорный, а разрыв на стыке — **0.041–0.042 с, то есть ровно
один кадр 24 fps, на всех 16 стыках без исключения**, и это то же самое число, что даёт
чистая копия. Стыков с разрывом больше кадра — ноль. Копия→перекод, перекод→перекод и
перекод→копия друг от друга неотличимы. Тяжёлым как есть ушёл один сегмент — тот, с
которого начали показ; его держать нельзя, иначе не появится картинка. tmpfs каталога
показа на пике — 382 МБ.

**🔴 Живой Q70D: перекодирование работает, а число 16 Мбит/с оказалось неверным.**
Смок `scripts/tvprobe.py`, тот же тяжёлый участок «Моаны 2» 13.3 ГБ, 200 с показа,
опрос приёмника 2 с (как в показе):

| прогон | что уехало на ТВ | BUFFERING | показ прошёл |
|---|---|---|---|
| без перекода, 4884 с | 21…28 Мбит/с | 186 опросов из ~300 | 102 с из 150 |
| перекод в 12 Мбит/с | 11…15.5 Мбит/с | 46 из 100 | 154 с из 200 |
| **перекод в 9 Мбит/с** | 6…10 Мбит/с | **13 из 100, после прогрева 3** | **179 с из 200** |
| перекод в 6 Мбит/с | 4…8 Мбит/с | 18 из 100, после прогрева 3 | 177 с из 200 |
| контроль: без перекода, лёгкий участок того же файла | 10…18 Мбит/с | 23 из 60 | 97 с из 120 |
| контроль: «Моана» 2016, 4.17 ГБ | 3…7 Мбит/с | **5 из 60, все на прогреве** | 113 с из 120 |

Что из этого следует и чего это стоило признать. **Перекодирование в 12 Мбит/с приёмник
не спасает**: он встаёт раз в 10–15 с, хотя ни один сегмент не уехал тяжелее 15.5. Тот же
приёмник встаёт и на 13–15 Мбит/с **вообще без всякого перекодирования** (контроль на
лёгком участке того же файла). Значит оценка «потолок декодера между 18 и 22» (§9) и
опущенная до 16 (§7.5) обе были завышены: 16 Мбит/с этот телевизор на этом релизе не
тянет. Инструмент при этом исправен — «Моана» 2016 через тот же смок идёт чисто.

Планка поставлена по замеру: перекодируем всё, что выше **10 Мбит/с** (`recode_at_mbit`),
и перекодируем в **9** (`recode_mbit`). Между 9 и 6 разницы нет — ниже девяти покупать
нечего, а чёткость теряется. На этом пороге перекодировать приходится 75 % «Моаны 2»
(335 сегментов из 447, 55.6 мин работы кодировщику на 100 мин фильма) — то есть для
такого релиза «выборочное перекодирование» честнее называть «почти сплошным», и оно всё
равно укладывается.

⚠️ `bitrate_warn_mbit` (16) владелец ставил как потолок ОТБОРА, и он остался 16 — планка
приёмника живёт отдельным ключом. Смешивать их нельзя: с выключенным перекодированием
`bitrate_warn_mbit` снова становится фильтром, и 10 отбраковало бы почти всё.

**Что стало с отбором релизов.** Потолок битрейта перестал быть фильтром: `is_candidate` и
`_Bench._trouble` теперь отбраковывают по `bitrate_hard_mbit` (25 Мбит/с) — по тому, что
перекодированием не спасти, а не по тому, что не тянет декодер. «Моана 2» 13.3 ГБ
(19 Мбит/с) снова становится дефолтом, 4K-ремукс на 49.9 Мбит/с — нет. В таблице релизов
у тяжёлого, но берущегося релиза стоит «перекодируем», а «тяжёлый» осталось за теми, кого
не берём. Честное разрешение по-прежнему решает среди годных (§7.4), играбельность
(образ диска, HEVC, выше жёсткого потолка) по-прежнему решает раньше него.

### 6.2.1 Голова прогона и дыры в придержании (06-08-2026, ночь)

Три 🔴/🟠-пункта из §6.2 закрыты, и цена оказалась не та, что прикидывалась.

**🔴 «Первый сегмент показа уходит тяжёлым как есть» — закрыт.** Первый сегмент каждого
прогона упаковки (старт, «Продолжить?», любая перемотка) назван **головой**
(`Recoder.opening`, зовётся из `Feed.restart` ПЕРВЫМ делом, до пробного прогона). Голова —
единственный кусок, который держат прямо под носом у показа: картинки в этот момент нет ни
одного кадра, и ждать тут значит стартовать, а не подгружаться. Заход за головой берёт
**ровно один** кусок (иначе срок считался бы по последнему и вышел бы `superfast`), идёт
`ultrafast` и **под `nice 0`**, а не 15: голову ждёт чёрный экран, а не запас впрок.
Ожидание ограничено `recode_head_wait` (12 с) — просрочено, копия уходит как есть.

⚠️ **Прикидка «сегмент 10 с за 2.3 с» была неверна вдвое-втрое.** Сетка по опорным кадрам
даёт сегменты 10–20 с, а к чистому кодированию добавляются подъём ffmpeg и чтение 50–80 МБ
из роя. Замер (стенд, «Моана 2» 13.3 ГБ, `ultrafast`):

| кусок | длина | `nice 15` | `nice 0` |
|---|---|---|---|
| v0 (31 Мбит/с) | 19.96 с | 8.05 с | **5.84 с** |
| v45 (18 Мбит/с) | 19.96 с | — | 6.03 с |
| v358 (26 Мбит/с, тяжелейшее место) | 16.00 с | — | 9.01 с |

**Что это стоило старту — замер на живом Q70D**, «Продолжить?» с 1:21:24 (v358, 22 Мбит/с),
опрос владеющим сендером раз в 2 с:

| `recode_head_wait` | старт (Enter → «играю») | голова | BUFFERING за прогон |
|---|---|---|---|
| 0 (как было) | 6–7 с | ушла копией, 43 МБ | **8 опросов из 38** |
| 12 | 6–10 с | перекод, 21 МБ, 5–8 с | **0 из 50** |

То есть плата за голову — **0–4 с к старту**, а не 4–13: копия того же места весит втрое
больше и телевизор качает её дольше, так что часть ожидания возвращается на LOAD→PLAYING.

**⚠️ Две дыры в придержании, каждая стоила прогона на живом ТВ.** Обе нашла не догадка, а
строка в журнале: `note()` теперь пишет, чем был занят кодировщик в момент, когда тяжёлый
кусок ушёл копией («тяжёлый v359 (26 Мбит/с) ушёл копией: показ 4884 с, край 359, заход не
идёт»). Без неё разбирать это пришлось бы по размерам файлов в журнале раздачи.

1. **Кусок за пределами текущего захода не держали вовсе.** Заход за головой берёт один
   кусок, а упаковщик за эти 5–8 с успевает выложить копией три следующих: `v359` (33 МБ),
   `v360` (28.7 МБ), `v361` (**65.7 МБ**). Показ — 27 опросов BUFFERING из 43. Теперь срок
   считается и за краем захода: остаток текущего плюс самый быстрый пресет до нужного
   куска, но не дальше `RUN_MAX` — дальше у кодировщика планов нет.
2. **Дыра МЕЖДУ заходами.** Придержание опиралось на фору `grace` (6 с) от подъёма
   кодировщика, а заход за головой шёл 8 с — и в зазор «заход кончился, следующий ещё не
   начался» уходило самое тяжёлое. Очередь идёт от места показа вперёд, то есть следующим
   кодировщик возьмётся ровно за этот кусок: держим, если он поспеет (`span/ultrafast` +
   подъём 3 с + запас 2 с) раньше показа.

После обеих правок тот же смок дал **0 опросов BUFFERING из 50** — позиция шла ровно по
2 с на опрос, ни одного нуджа сторожа, ни одного «ушёл копией».

**🟠 Планка подтверждена вторым релизом — остаётся 10/9.** Взят другой фильм:
`Cars.3.2017.1080p.BluRay.DTS.x264-Geek` 13.0 ГБ, **видео 14.10 Мбит/с**, 1920×804,
ref=5, bframes=8 — то есть по весу близнец «Моаны 2», а по энкоду совсем другой.
Смок `tvprobe`, тяжёлое место 3880 с, 100 с показа:

| прогон | BUFFERING | вердикт |
|---|---|---|
| без перекода | **36 опросов из 50** | встал на 3904.6 (v308), 8.1 с при запасе 20–60 с |
| перекод в 9 Мбит/с | 19 из 50 | встал на 3894.4 (v307), 8.1 с; тяжёлых ушло как есть 1 |

Значит дело не в свойствах энкода «Моаны 2»: **это потолок приёмника**, и `recode_at_mbit`
поднимать назад нельзя.

**Дискриминатора в паспорте нет — искали честно.** Сняты полные паспорта ffprobe (потоки,
формат, покадровые окна, разбор SPS через `trace_headers`) для пяти файлов: проблемной
«Моаны 2» 13.3 ГБ, чистых «Моаны» 2016 и YTS-mp4, YTS-repack и «Тачек 3». Убито замером:

* **1920×960 / SAR / кроп — не при чём.** Чистый YTS-mp4 (§7.5 п.9) — ровно 1920×960,
  SAR 1:1, DAR 2:1, `frame_cropping_flag=0`, High@L4.1, ровно 24 fps, и SPS у него почти
  байт-в-байт как у проблемного (`max_num_ref_frames=4`, `max_dec_frame_buffering=4`,
  `max_num_reorder_frames=2`, CABAC, 8x8dct, `weighted_pred=1`, `weighted_bipred_idc=2`).
* ⚠️ **Запись «refs=1» в §6.2 и §9 была ошибкой чтения.** `ffprobe -show_streams` отдаёт
  `refs: 1` для **всех** файлов без исключения — это поле декодера, а не SPS. Настоящее
  значение берётся только разбором SPS: у проблемного 4, у ЧИСТОЙ «Моаны» 2016 — 5.
* **HRD/VBV — не при чём:** `nal_hrd` есть ровно у одного файла, и это чистая «Моана» 2016.
* **Interlaced-флагов нет ни у кого** (`frame_mbs_only_flag=1`, `progressive`).
* **Длина и вес GOP, число дорожек, fps, chroma, глубина — не делят набор.**
* Формально делит только глубина B-пирамиды (10 против 3 и 1), но её опровергает
  собственный замер §6.2: перекод в libx264 `veryfast` (bframes=3, строгий VBV) при капе
  12 Мбит/с всё равно давал 46 BUFFERING из 100. Сложность декодирования — не механизм.

Остаётся единственное, что делит набор: **битрейт видеодорожки** (14.33 и 14.10 против
4.49 и 2.25). Перекодируем по нему, и порог 10 остаётся.

**🟡 Побочная находка, которая закрывает поправку «контейнер → ТВ».** Считать надо не
контейнер, а `streams[v].tags.BPS` (mkvmerge пишет его в голову файла, ffprobe уже читает)
плюс битрейт ВЫБРАННОЙ звуковой дорожки. У «Моаны 2» контейнер 19.16, а уезжает 14.78 —
разрыв 4.38, ровно та поправка, которую `Weights.calibrate` сейчас набирает вслепую за
8–10 сегментов. И она **не константа**: у «Тачек 3» разрыв 2.86, у «Моаны» 2016 — 0.63.
Не сделано (см. ниже).

**Осталось несделанным.**

* 🔴 **Полный фильм на живом ТВ не прогнан** — только смоки по 1.5–4 минуты. Число
  «0 BUFFERING из 50» на 100-минутном фильме не проверено.
* 🟠 **«Тачки 3» с перекодом дают 19 опросов BUFFERING из 50, а не ноль.** У «Моаны 2» в
  том же режиме — ноль. Разница не разобрана: у «Тачек» тяжёлое место идёт 40–46 Мбит/с
  по контейнеру (у «Моаны» 26–31), кодировщик там брал `veryfast` и один тяжёлый кусок
  всё-таки ушёл как есть. Кандидат — поправка `extra`, поставленная руками (2.9), и
  выбор пресета на пределе.
* 🟠 **Голова прогона считается заново на каждой перемотке.** Пять перемоток подряд — пять
  ultrafast-заходов по 6–9 с, каждый отбирает кодировщик у работы впрок. На живом ТВ это
  не проверялось.
* 🟡 Поправку «контейнер → ТВ» из ffprobe (`BPS` видео + выбранная дорожка) не сделали:
  `Media` этих полей пока не несёт. Это же убрало бы 🟡-грабку «первый заход кодировщика
  берёт кусок, который тяжёлым не был», и `bitrate_hard_mbit` стоило бы считать так же —
  иначе релиз с десятью дорожками выглядит на 4–5 Мбит/с тяжелее, чем есть.
* 🟡 Ожидание головы (6–9 с) можно спрятать целиком: §7.2 уже греет под меню карту и
  начало файла — туда же просится и перекод головы дефолтного релиза. Не сделано.

## 7. Порядок работ и приёмка

**Этап A. Свести репу и стенд.** Ветка `fix/tv-live-load` вливается в master; дефолты в репе
(`state.py`, `install.sh`) приводятся в соответствие с настройкой, проверенной на ТВ
(`hls_readrate`/`hls_window` сейчас живут только в `/etc/torrcast/config.json` на стенде —
это расхождение с принципом «деплой только из репы» и чинится первым).

**Этап B. Устойчивость (§6).** Полный фильм на Q70D. Без зелёного результата дальше не идём.

**Этап C. UX (§2) и консоль (§3).**

**Этап D. Время и прогресс (§4).**

**Этап D2. Транспорт и развязка (§5).** HTTP по IP → проверка прямого пути из сегмента ТВ →
чистка серта, DNS-rewrite, доставки и следов на прод-ингрессе. После этого справедливо
утверждение «torrcast — это одна ВМ дома», и оно проверяется буквально: выключить AdGuard-rewrite
и убедиться, что каст жив.

**Этап E. Финальная приёмка — целиком на живом ТВ:**
- [ ] `cast моана 2` → картинка на экране, максимум два Enter'а, ни одного упоминания файлов
- [ ] фильм целиком до титров, без зависаний, без ручных вмешательств
- [ ] сериал: серия целиком → автопереход на следующую → `stop` → `cast <сериал>` продолжает
- [ ] русский ввод в вопросах работает с ssh из коробки (Мак, без `stty`)
- [ ] прогресс по фазам виден; «старт NN с» = момент появления картинки
- [ ] перемотка пультом вперёд и назад не роняет показ
- [ ] на ТВ виден таймлайн: общее время фильма и позиция; пульт мотает в любую точку (§2.1)
- [ ] `stop`/`status`/`doctor` корректны, юнит и tmpfs не текут
- [ ] HTTP по IP: ни серта, ни DNS, ни облака в пути — и это доказано отключением rewrite
- [ ] на прод-ингрессе и в AdGuard не осталось следов torrcast; чужие домены живы

**Правило сдачи §9 v1 остаётся в силе**, с одной поправкой: формулировка «на mock работает»
теперь не засчитывается вообще ни в каком виде. Доказательство — только живой Q70D.

## 7.1 TODO после v2 (отложено владельцем)

- ~~**Холодный старт 21–26 с при пороге 15**~~ — ✅ закрыто 06-08, см. §7.2. Гипотеза
  «весь разрыв — первое чтение Cues у роя (13–24 с)» **не подтвердилась**: рой отдаёт
  Cues за 2–6 с, а 13–24 с были нашим квадратичным разбором карты.
- ~~**Карта опорных кадров только для mkv**~~ — ✅ закрыто 06-08. mp4 разбирается по
  `stss`/`stts`/`ctts` из `moov` (`torrcast/mp4.py`), общее с mkv уехало в
  `torrcast/keymap.py`, контейнер узнаётся по первым байтам той же головы. Живая сверка на
  `Moana.2.2024.REPACK.1080p.BluRay.x264.AAC5.1-[YTS.MX]` (1.97 ГБ, один сид): карта —
  1304 опорных кадра, три Range-запроса, 2.36 МБ, 15.9 с на холодной раздаче и 0.05 с из
  кэша роя. Сверено с `ffprobe` в четырёх местах фильма (0…5, 25, 50 и 96 мин): 144
  опорных кадра, расхождений по времени и по байтам — ноль. `scripts/gridcheck.py` со
  слотов 0, 5 и 7: байты видео v7…v12 совпали во всех трёх — нарезка mp4 детерминирована
  так же, как mkv. Грабли, стоившие отладки: `elst` пропускать нельзя (у YTS `media_time`
  = два кадра, а ремукс mkv→mp4 вставляет наоборот пустую правку в 6 мс, заданную в
  масштабе фильма); дети бокса перечисляются генератором, иначе ради дорожки видео
  вычитывается дорожка звука (5.26 МБ вместо 2.36).
  ⚠️ Замечено при сверке и осталось как есть: при упаковке **от нуля** метки всего фильма
  у mp4 с `elst` уезжают на `media_time` (0.083 с у «Моаны 2» от YTS) — то же явление,
  что кадр 0.042 с у mkv (§6.0), и по той же причине: ffmpeg не пускает dts ниже нуля.
  На нарезку это не влияет (байты сегментов совпадают), но при сверке границ помнить надо.
- Порог «сегмент ≥10 с» эмпирический (§6.0) — внутренний механизм приёмника не доказан.
- ~~**Дефолт меню франшизы = самая ранняя картина**~~ — ✅ закрыто 06-08. Список остался
  хронологическим, цифра в скобках теперь указывает на самую живую картину: сиды у той
  раздачи, которая реально поедет на ТВ (верх `rank_releases`, и только если он годен —
  негодный верх весит ноль). На живой выдаче 06-08: «моана» → `[2]` «Моана» 2016 (222
  сида) вместо немой документалки 1926 года (один VHS-рип, 5 сидов, негодный); «матрица»
  → `[1]` без изменений (170); «аватар» → `[9]` «Пламя и пепел» 2025 (292); «тачки» →
  `[4]` «Тачки 3» 2017 (121), потому что у «Тачек» 2006 в выдаче нет ни одной играбельной
  раздачи — 4K-ремукс на 49.9 Мбит/с выше потолка декодера, остальное образы DVD.
  Прогрев под меню теперь начинается с дефолта (`warm_order`), иначе Enter попадал бы в
  непрогретую картину и §7.2 (0.6–2.7 с) не держался бы. Без tty — по-прежнему честный
  отказ: дефолт имеет смысл только рядом с напечатанным списком, а смотреть его некому.
- ~~**Ранжирование релизов иногда ставит первым mpeg4**~~ — ✅ закрыто 06-08. В ключ
  сортировки добавлена ступень «старьё» между годностью и сидами: явные маркеры имени
  (XviD/DivX/`.avi`/DVDRip/VHSRip/SATRip/TVRip/DVDScr — парсер наконец читает MPEG-4 как
  кодек) плюс «имя не называет ни разрешения, ни кодека, а размер даёт < 4 Мбит/с».
  Порог измерен, а не угадан: из 264 раздач живой выдачи по пяти запросам удалось достать
  `.torrent` и заглянуть внутрь у 36 — все восемь `.avi` не называют ни разрешения, ни
  кодека, у пяти полнометражных потолок 3.5 Мбит/с, а ближайший подтверждённый `.mkv` с
  такой же безымянной шапкой — 5.4 Мбит/с. На этой выборке эвристика понижает 5 `.avi` из
  5 полнометражных при 0 ложных срабатываний. «Моана 2»: первым был 1.46-гигабайтный
  `Moana.2.2024.WEB-DLRip.ELEKTRI4KA.avi` с 221 сидом, стал WEB-DL-AVC 3.14 ГБ со 140 —
  сэкономлены 2–5 с на отбраковке ffprobe. ⚠️ Осталось: у сериала размер раздачи — это
  весь сезон, битрейтом судить нельзя, и подтверждённый `.avi` «Легенды об Аанге» 2024
  по-прежнему ловит только ffprobe. Чинится счётом серий.

## 7.2 Холодный старт: где он был на самом деле (замеры 06-08-2026)

Метрика: **от Enter'а после последнего вопроса до готовности LOAD** — манифест и первый
сегмент. Меряет `scripts/startbench.py` настоящим `cast` с приёмником `mock`, ни одного
пакета к ТВ. К этому числу на живом ТВ добавляется LOAD→PLAYING (0.7–1.5 с, замерено).

| фаза после ответов | было | стало |
|---|---|---|
| запуск юнита + процесс показа | 0.36 | 0.15 |
| карта опорных кадров (сетка) | 20.9 | 0.06 (из кэша прогрева) |
| раздача + пробный прогон + старт ffmpeg | 0.01 | 0.01 |
| первый сегмент дописан | ~0.4 | 0.37–1.41 |
| **итого** | **≈21.7** | **0.63–2.68** |

Пять прогонов: «Моана 2» холодная — 0.66 и 1.44 с, она же прогретая — 0.63 с, «Тачки 3»
(первое касание роя) — 0.71 с, «Интерстеллар» (первое касание) — 2.68 с. Разницы между
холодным и прогретым по сути не осталось: всё, что не рой, стало нулём, а рой греется под
меню.

⚠️ Замеры сделаны в худшем случае — ответы на вопросы подаются **мгновенно**, то есть
прогреву под меню не дано ни секунды форы. Весь разброс (0.2 с против 1.2 с до манифеста)
— это ровно он: успела ли карта опорных кадров лечь в кэш до Enter'а. Человек, который
читает меню хотя бы пару секунд, всегда попадает в нижнюю границу.

**Что оказалось внутри «13–24 с чтения Cues у роя».** Разбор карты выбирал дорожку видео
внутри списочного включения, то есть **на каждую точку Cues**, а сам выбор линеен по всем
точкам: карта разбиралась квадратично. «Моана 2», 7274 точки — 18.5 с чистого процессора
(systemd показывал 20.8 с CPU у юнита). Ровно эти секунды и списывали на рой. Хуже того,
замок прогрева снимался до записи кэша, поэтому CLI и юнит разбирали одну и ту же карту
параллельно и платили квадрат дважды.

**Сам рой** отдаёт Cues за 2–6 с (замер `scripts/cuesprobe.py`: «Моана» 2016 — 2.1 с,
«Моана 2» — 4.3 с, «Моана 2» 14 ГБ — 5.7 с), и 95 % этого времени приходилось на чтение
4 МиБ головы, а не хвоста: пока голова не приехала, запрос к Cues даже не отправлен.

Сделано:

1. Дорожка видео считается один раз (`stream.film_keys`).
2. Замок прогрева держится до записи кэша — сосед ждёт готовое, а не читает второй раз.
3. Голова читается прогрессивно: сначала 256 КиБ (SeekHead и Info лежат там), полные
   4 МиБ — только если не хватило. Тело Cues берётся одним куском вместо «заголовок,
   потом тело». Заходов к рою два вместо трёх, байт — 0.45 МБ вместо 4.4 МБ.
4. Под меню греется не только карта, но и **начало файла** (`stream.pull_head`, 32 МБ):
   это ровно то, что следующим действием прочитает ffmpeg. Прогрев отменяется, как только
   показ отказался от релиза, — чтобы не отъедать полосу у выбранного.
5. `ConnectionsLimit` TorrServer поднят с 25 до 100 (`install.sh`): потолок первых секунд.
6. Секундомер фаз живёт в коде (`torrcast/timing.py`, `TORRCAST_TIMELINE`) и склеивает CLI
   с юнитом — иначе разбивку старта по двум процессам не увидеть.

Детерминированность нарезки не тронута: сетка по-прежнему считается от карты опорных
кадров (§6.0), тесты `test_hls.py` зелёные.

**Остаточный минимум 0.6–0.7 с** и почему он не сжимается дальше: 0.15 с — `systemd-run`
плюс старт второго интерпретатора (показ обязан пережить закрытие ssh, §3); 0.06 с —
чтение кэша карты и построение сетки на 481–525 сегментов; 0.45 с — ffmpeg обязан открыть
вход, прочитать первый сегмент целиком и закрыть его, иначе отдавать приёмнику нечего.
Ниже этого — только рой, и он уже прогрет под меню.

## 7.3 Продолжение с середины: прогрев под вопросом «Продолжить?» (замеры 06-08-2026)

Метрика та же, что в §7.2: **от Enter'а до готовности LOAD** (манифест плюс первый
сегмент — тот, что лежит в месте позиции, а не `v0`). Приёмник `mock`, к ТВ ни одного
пакета. Условия: «Моана 2», 3.4 ГБ mkv, продолжение с 30:00, раздачи TorrServer сброшены
перед каждым прогоном, кэш карты тёплый — то есть фильм уже играли, а рой холодный.

| прогон | до | после |
|---|---|---|
| готовность LOAD | 3.66 · 3.51 · 3.36 · 2.76 · 5.36 · 3.29 (медиана **3.4**) | 1.49 · 1.34 · 2.64 · 1.84 (медиана **1.7**) |
| манифест готов | 1.79 · 2.68 · 2.69 · 2.10 · 3.52 · 1.85 (медиана **2.4**) | 0.66 · 0.67 · 1.78 · 1.19 (медиана **0.9**) |

На этом пути прогрева не было вовсе: `cast` спрашивал, а раздачу поднимал уже юнит —
после Enter'а. Теперь под вопросом работает `cli._Resume`: TorrServer, карта, заголовок
файла (8 МБ) и **место позиции** (32 МБ по смещению из карты опорных кадров). В ленте фаз
обе грелки успевают лечь до Enter'а: голова за 0.3–0.5 с, позиция (смещение 944 889 955,
это 30-я минута) за 1.3–2.2 с.

Смещение берётся из карты, а не долей «позиция от длительности умножить на размер файла»:
битрейт по фильму гуляет вдвое, и промах в процент трёхгигабайтного файла — 30 МБ мимо.
Ради этого карта теперь несёт абсолютные смещения кадров (у mkv они лежат в файле
относительно начала `Segment`), а кэш карт — их же; кэш прошлой версии остаётся годным
для сетки, просто грелка позиции на нём не работает.

⚠️ **Прогрев обязан прекращаться в момент ответа.** Первая версия доигрывала — и стала
хуже, чем ничего: второй читатель того же места через TorrServer отбирает полосу у самого
показа. Замер: пробный прогон 0.56 → 1.92 с, готовность LOAD 3.5 → 4.8 с.

~~Остаток на потом: заголовок съедает бюджет~~ — ✅ закрыто 06-08 ночью. `HEAD_OPEN`
теперь считается по контейнеру: mkv — 1 МБ, mp4 — 8 МБ (там `moov`), неизвестный
контейнер — 8 МБ. Контейнер известен по первым байтам той же головы, которую читает карта
опорных кадров, и доезжает до прогрева даром (`KeyMap.kind` → `FilmKeys.kind` → кэш карт);
для карт, снятых прошлой версией, контейнер называет имя файла раздачи (`container_of`).

Замер на стенде («Моана 2», 3.4 ГБ mkv, продолжение с 30:00, рой сброшен перед каждым
прогоном, приёмник mock, ни одного пакета к ТВ):

| раздумье | что мерим | было (8 МБ) | стало (1 МБ) |
|---|---|---|---|
| 4 с | голова прогрета за | 1.54 · 0.34 · 1.15 | **0.25 · 0.22 · 0.22** |
| 4 с | место позиции прогрето к Enter'у | −0.00 · −1.32 · −0.17 с | **−0.67 · −0.90 · −0.88 с** |
| 4 с | готовность LOAD | 1.48 · 1.48 · 1.48 | 1.53 · 1.93 · 1.78 |
| 1 с | готовность LOAD, 6 прогонов | 1.56 · 2.37 · 2.87 · 3.12 · 3.58 · 8.36 | 1.77 · 2.57 · 2.87 · 3.68 · 3.78 · 10.17 |

Что из этого следует честно:

* сама голова перестала быть статьёй расхода — 0.22 с вместо 0.34–1.54 с, и грелка
  позиции стартует почти на секунду раньше. При раздумье 3–5 с обе грелки теперь
  успевают с запасом, тогда как раньше позиция ложилась ровно в момент Enter'а (−0.00 с);
* готовность LOAD при коротком раздумье **не изменилась**: медианы 3.0 против 3.3 с при
  разбросе 1.6–10 с в обоих наборах. На одной секунде раздумья не успевает ни та версия,
  ни эта, и всё решает рой. Выдавать это за улучшение нельзя;
* готовность LOAD при раздумье 4 с не деградировала (1.5 против 1.8 с — тот же разброс);
* из роя на каждое продолжение тянется на 7 МБ меньше — это уже не шум, а арифметика.

⚠️ **Грабля замера:** `--think` в `scripts/startbench.py` до 06-08 не работал вовсе. Без
терминала `ask_line` штатно не спрашивает (§3), поэтому ответы уходили мгновенно, сколько
ни задерживай запись в stdin, — «человек думает» на пайпе не воспроизводится в принципе.
Теперь замер запускает `cast` под pty, и пауза настоящая. Там же: «первый сегмент дописан»
ищет сегмент того места, откуда играем, — mock открывает поток через `ffmpeg -ss`, тот
дёргает ещё и начало плейлиста, и по `v0` замер показывал не то.

## 7.4 Честное качество и ревью v2-диффа (ночь 06-08-2026)

### Качество на экране — из ffprobe, а не из имени раздачи

Живая выдача «моаны 2»: верхом отбора стоит `WEB-DL-AVC`, 3.14 ГБ, 140 сидов, и внутри у
него **1150×574**, а вторым лежит настоящий 1080p — 13.3 ГБ, 121 сид, 17.8 Мбит/с (под
потолком 20). Строка запуска печатала заявку имени, то есть молчаливую подмену (§1 v1).

Сделано:

* разрешение в строке «играю …», в `cast status` и в состоянии (`Entry.quality`) берётся
  из ffprobe **того файла, который уехал на ТВ**; заявка имени туда не попадает;
* судим по кадру целиком, а не по одной высоте: 1920×800 — это обрезанный скоуп и
  честный 1080p, 1150×574 — SD с любым именем (`Media.frame`, ffprobe читает и ширину).
  Без этого каждый широкоформатный фильм объявлялся бы враньём;
* среди прогретых кандидатов подтверждённое разрешение решает: верх «на деле 574p» при
  живом соседе, который обещает 1080p и это подтверждает, уступает ему место. Ждём соседа
  не дольше `HONEST_BUDGET` (12 с) — он греется с той же секунды, что и верх, поэтому
  платим за разницу двух ffprobe, а не за прогрев;
* обсиженность осталась главным критерием **среди честных**, потолок 20 Мбит/с не тронут,
  `--release N` неприкосновенен. Молчаливых подмен нет в обе стороны: и подмена, и отказ
  от неё печатают строку.

Живая проверка на стенде (приёмник mock, `tv: ""`, ни одного пакета к ТВ):

| запрос | что было | что стало |
|---|---|---|
| `моана 2` | `«Моана 2» (2024) · 574p` | `релиз №1 на деле 574p — беру №2 (настоящий 1080p)` → `1080p`, 5.6 с |
| `моана` | `«Моана» (2016) · 1080p` | без изменений (верх честен, соседей не трогаем) |
| `тачки 3` | `1080p` | без изменений |
| `киберпанк` | `релиз №1 не годится (hevc) — беру №2` | без изменений, `s1e1 · 1080p` |
| `cast status` | позиция без качества | `играю «Моана 2» · 1080p — 0:02:33 / 1:39:37` |

### Ревью диффа `master..v2/stability`: что починено

* **обрыв упаковки считался на опрос, а не на прогон.** Пока держит защита «не толкаемся»
  (2 с), перезапуска не происходит, а потоки раздачи приходят каждые 0.2 с — один и тот же
  труп съедал все три попытки за 0.8 с, и показ умирал, ни разу по-настоящему не
  перезапустив ffmpeg. Ровно так выглядит выроненная раздача: вход мёртв, ffmpeg умирает
  внутри этих двух секунд;
* **`Feed.stop()` не закрывал раздачу.** Поток, спящий в `segment()` до двух минут, мог
  проснуться уже после конца показа и поднять новый ffmpeg в каталог, отданный следующей
  серии;
* **прогретые раздачи оставались в TorrServer** при любом выходе мимо `keep_only` —
  Ctrl-C на вопросе, запуск без терминала, «годного релиза нет». Наш процесс умирает,
  раздачи качаются в чужой RAM до перезапуска сервера;
* **бегущее время прогресса умирало на первой же пустой фазе**: поток тика выходил на
  `phase("")`, а заводится он только пока его нет вовсе. Владелец видел замершее
  «метаданные (DHT)… 0 с» — то самое «повисло или нет», ради которого написан §4;
* **осечка приёмника в `finally`** уносила с собой `feed.stop()` и `server.stop()`.

### Ревью: что НЕ починено и почему (для следующей ходки)

Статус после живого прогона 06-08-2026 (день, ветка `v2/stability`): **закрыты 1, 2, 3,
4, 5, 6**; остаётся 7 (мелочи) и один **новый** 🔴, вскрытый живым стыком серий (см.
«Стык серий» ниже). Разбор — в «Честном крае упаковки» и «Сторож и стык серий».

1. ✅ **закрыто** (см. ниже). 🔴 **Перемотка назад глубже окна при упаковке от нуля** висит две минуты и отдаёт 404,
   после которого приёмник не берёт LOAD ещё пару минут. Место: `stream.py`, `Feed._steer`,
   условие `packer.first <= slot <= frontier + self.ahead`. Наивная починка («ждать только
   впереди края») **проверена и откачена**: замер на стенде дал 20 перезапусков упаковки за
   100 с показа против 1 — потому что `Packer.frontier()` считает край **глобом каталога**,
   где лежат и куски прошлых прогонов, и «ниже края» регулярно оказывается то, что этот
   прогон ещё не дописал. Чинить надо с другого конца: сначала честный край прогона
   (`Packer.edge`, обновляемый в `publish`), и только потом граница ожидания. Это ломает
   фикстуру `fake_packer` и пару тестов — работа на светлое время, не вслепую ночью.
2. ✅ **закрыто на живом ТВ** (см. «Сторож и стык серий»). 🔴 **`front()` врёт после перемотки назад** по той же причине (глоб каталога), а на этом
   числе висит защита сторожа приёмника от ложного нуджа (`_nudge`, `front - pos <
   READY_AHEAD`). Плюс `_peak` никогда не опускается, поэтому нудж и `_reload` целятся в
   место, откуда владелец только что ушёл.
3. ✅ **закрыто на живом ТВ**, но экран на стыке это не починило (см. «Сторож и стык серий»). 🟠 **Сериал поднимает новое соединение pychromecast на каждую серию.** `_play` создаёт
   приёмник сам, а `stop(quit_app=False)` на стыке серий не рвёт соединение — на третьей
   серии их три, и это ровно тот «второй сендер», от которого показ гаснет (`cast.py`,
   докстринг класса). Починка — приёмник на весь юнит, а не на серию (~10 строк в
   `_cmd_worker`/`_play`), но проверяется она только живым стыком серий на ТВ.
4. ✅ **закрыто.** 🟡 `_await_playing` ждёт 120 с, а бюджеты юнита в сумме больше (60 + 90 + 60) — CLI
   может погасить показ, который вот-вот начался. Число подбирать замером.
5. ✅ **закрыто.** 🟡 Сегменты **впереди** приёмника после перемотки назад не чистятся никогда (`prune`
   смотрит только назад). Самоограничивается следующей перемоткой вперёд; на стенде
   `/dev/shm` 63 ГБ, до беды — десятки откатов подряд.
6. ✅ **закрыто.** 🟡 Юнит выходит кодом 2 на штатный `cast stop` (`_on_term` кидает `KeyboardInterrupt`),
   и systemd помечает его `failed`.
7. 🔵 Мелочи: `MockReceiver.stop()` не закрывает временный файл лога; `film_keys` не
   освежает mtime замка (замок 60 с против 2–6 с чтения — запас есть); кэш карт
   `/var/lib/torrcast/keys/` не подчищается никогда; `tmp`-имя кэша карты фиксировано на
   URL (два писателя без замка могут переписать друг друга); `pack_start` держит
   `Feed.lock` до 60 с; `_both_languages` квадратичен по числу картин.

Проверено ревью и признано в порядке: §6.0-нарезка целиком (`Grid`, `-segment_times`,
докатка в `slot-1`), разбор mkv и mp4, `timing.py`, манифест и Range-раздача, атомарная
запись состояния, восстановление termios.

### Честный край упаковки: 🔴 перемотки назад закрыт (ночь 06-08-2026)

**Правда о крае теперь одна и растёт только на факте.** `Packer.edge` — последний
сегмент, который **этот прогон** переименовал наружу (`publish`); до первого куска он
равен `first - 1`. Глоб каталога (`frontier`) на решениях об упаковке больше не стоит
нигде: он остался только под `Feed.front` — запасом показа для сторожа приёмника,
который доказан на живом ТВ и правится только с владельцем (пункт 2 выше).

Почему обе прошлые попытки ломались об одно и то же место: каталог показа общий на весь
фильм, и куски прошлых прогонов в нём лежат **честные** — сетка одна и детерминированная
(§6.0), под именем `vN` и до, и после перезапуска одно и то же место фильма. Поэтому
отдавать их приёмнику можно и нужно, а вот считать по ним, докуда дошла упаковка,
нельзя: глоб уводил край то вперёд (запрос далеко назад попадал в «вот-вот допакуется» и
висел до 404), то назад (20 ложных перезапусков за 100 с). Честный край разводит эти два
вопроса окончательно.

Правило `Feed._steer` стало симметричным: ждём только `edge < slot <= edge + ahead`, всё
остальное при живой упаковке — перемотка, и вперёд и назад лечатся одинаково.

⚠️ **Грабля, найденная замером, а не рассуждением.** `_steer` сам зовёт `publish`, и
кусок, которого секунду назад не было, появляется **внутри этого решения**: приёмник
идёт вплотную за упаковкой и просит сегмент за мгновение до того, как тот закрылся. Без
проверки «а не появился ли он ровно сейчас» такой запрос выглядит как «ниже края, файла
нет», то есть перемоткой назад, — на стенде это давало перезапуск упаковки на каждом
четвёртом сегменте ровного показа (v5, v9, v13…). Проверка стоит сразу после `publish`.

Доказательство — `scripts/seekcheck.py`: локальный потребитель ходит по http на
`127.0.0.1` в настоящие `Feed` + `HlsServer`, ни одного пакета к ТВ. Стенд CT501,
синтетический ролик 600 с / 1280×720 / 3 Мбит/с, сетка по опорным кадрам 10 с,
`keep=120`, `burst=60`, `readrate=1`:

| сценарий | до (HEAD 977594d) | после |
|---|---|---|
| упаковка с 0, показ ушёл на 150 с, запрос v0 | **404 после 60.1 с** (в проде — 120 с) | **200 за 0.9 с** |
| упаковка с 300 с, показ на 450 с, запрос v25 (ниже старта прогона) | тот же 404 | **200 за 1.7 с** |
| перемотка вперёд на v48 + префетч v49–v51 | 200 | **200 за 3.0 с**, префетч за 0.2–0.4 с |
| ровный показ 15 сегментов подряд | 1 прогон | **1 прогон** (ложных перезапусков 0) |
| 404 за прогон | 1 | **0** |
| tmpfs в момент отката | **79 МБ и растёт** | **29 МБ** (пик за прогон 54 МБ) |

Заодно закрыты три 🟡:

* **уборка вперёд** (пункт 5). `Feed.prune` убирает не только окно позади, но и то, что
  осталось от прошлого места показа: граница — `max(позиция, edge) + ahead`, то есть
  запас текущего прогона и префетч не задеваются никогда, а после отката память
  освобождается на следующем же опросе (54 → 29 МБ в таблице выше). Упаковки нет вовсе —
  вперёд не трогаем ничего: без прогона край неизвестен, а гадать тут уже пробовали;
* **код выхода** (пункт 6). SIGTERM поднимает `_Stopped` (наследник `KeyboardInterrupt`,
  чтобы раскрутка шла через тот же `finally` и позиция писалась как раньше), а `main`
  отдаёт на нём 0. Замер на стенде через `systemd-run`: было `exit-code / код 2` и
  `Failed with result 'exit-code'`, стало `success / код 0`, причём строка из `finally`
  печатается в обоих случаях;
* **бюджет старта** (пункт 4). `cli.START_BUDGET` — не «побольше», а **сумма потолков**
  всех фаз юнита до первого `PLAYING`: `WORKER_META` 60 + `WORKER_DUR` 90 + `KEYS_WAIT`
  40 + `PILOT_TIMEOUT` 60 + `START_TIMEOUT` 90 + 10 на прочее = **350 с**. Потолки
  проставлены в самих вызовах, а не унаследованы от умолчаний, и тест держит
  неравенство. Ждать столько не молчаливо: `Progress` показывает живую фазу, а любая
  честная неудача убивает юнит раньше, и CLI печатает причину из журнала.

Регресс закрыт тестами (`tests/test_hls.py`, `tests/test_play.py`): перемотка назад ниже
края, куски прошлых прогонов не двигают край, кусок, допакованный этим же опросом, не
считается перемоткой, уборка вперёд и её отсутствие без упаковки, код выхода на штатной
остановке, согласие бюджетов. Фикстура `fake_packer` теперь спрашивает край у теста
(умолчание — «что лежало в каталоге на момент создания»), и куски, положенные после
создания, краем уже не считаются — ровно этим честный край отличается от глоба.

**Осталось из §7.4:** пункт 7 (🔵 мелочи). 🔴 «первый прогон упаковки следующей серии
умирает мгновенно» закрыт — см. ниже, умирала не та упаковка.

### Сторож и стык серий: живой прогон на Q70D (06-08-2026, день)

Прогон шёл настоящим `cast` на живом Q70D 192.168.100.102. Перемотки и паузы подавал
**владеющий сендер** — вторым pychromecast их не подать вовсе (приёмник считает второе
соединение тем же `sender-0`), а кнопку на пульте агент нажать не может. Для этого
заведён `TORRCAST_CTL=<файл>`: одна строка (`seek 1200` / `pause` / `play`) исполняется на
ближайшем опросе тем же объектом, что держит показ, и приёмнику приходит той же
MEDIA-командой, что и с пульта. Без переменной этого кода нет.

#### 🔴 Пункт 2 закрыт: сторож смотрел не туда и целился не туда

Дефект воспроизведён дважды подряд, а не выведен рассуждением.

* **`front()` врал.** Откат с 31:31 на 10:00 при упаковке, идущей с 29:51: в журнале
  `показ 600 · упаковано 2010 · впереди 1410 с`, при том что перед приёмником не лежало
  ни одного куска — 2010 давал глоб каталога по кускам прошлого прогона. То же на mp4:
  `показ 300 · упаковано 2509 · впереди 2209 с`. Это ровно разрешение сторожу дёргать
  показ там, где дёргать нельзя.
* **`_peak` не опускался.** Откат на 10:00, показ шёл чисто 18 с, потом ребуфер — и
  сторож выкинул фильм **обратно на 31:31** (нудж целится в `_peak + STALL_SKIP·попытки`).
  Второй случай в том же прогоне: откат 1813 → 1795, через 20 с показ выкинуло на 1859.

Починены **входы** сторожа, логика нуджа, пороги и STALL не тронуты:

* `Feed.front(позиция)` считает запас **от приёмника и по цепочке готовых кусков подряд**:
  разрыв цепочки — конец запаса, куска под позицией нет — запаса ноль. Глоб каталога
  (`Packer.frontier`) на решениях показа не стоит теперь нигде.
* `_peak` идёт за человеком: прыгаем мы только вперёд, значит позиция, уехавшая назад
  глубже `REWIND` (8 с), — это перемотка, и максимум опускается вместе с ней.
* Позицию для `front` `_hold` берёт с прошлого опроса. На решение это не влияет: нудж
  срабатывает только после 8 с неподвижности, то есть когда прошлая позиция и есть
  сегодняшняя, — а сразу после перемотки счётчик подвиса обнулён самой перемоткой.

| глубокий откат (упаковка с 29:51 → запрос 10:00) | до | после |
|---|---|---|
| `front` сразу после отката | `упаковано 2010 · впереди 1410 с` | `упаковано 600 · впереди 0 с` |
| картинка вернулась | 8.1 с | 6.1 с |
| 404 за прогон | 0 | 0 |
| перезапусков упаковки на 3 перемотки | 3 | 2 (откат назад в окно не перепаковывает) |
| куда ушёл показ за следующие 100 с | **выкинуло на 31:31** | 600 → 685, монотонно |

Нудж как таковой жив и по-прежнему лечит настоящий подвис: на «Моане 2» 13.3 ГБ он
сработал за прогон шесть раз (`619 → 627`, `688 → 696`, `699 → 707` …) — каждый раз на
кусок вперёд от **текущего** места, а не назад в покинутое.

#### 🟠 Пункт 3 закрыт, но экран на стыке серий это не починило

Стык s1e5 → s1e6 «Киберпанка», замер `ss` раз в 10 с:

| стык серий | до | после |
|---|---|---|
| соединений pychromecast к ТВ | **2** (сокет прошлой серии не рвался) | **1** |
| пустой экран на стыке | 15.3 с | 16.1 с |

Соединение теперь одно на весь юнит (`_cmd_worker` создаёт приёмник и передаёт его каждой
серии). Но **второй сендер оказался не причиной чёрного экрана**, и это выяснилось только
живьём. Настоящая причина видна в журнале обоих прогонов, до и после:

```
11:01:31.930 упаковка с 0.0 с                                  ← старт s1e6
11:01:32.159 упаковка оборвалась (нет вывода) — начинаю заново, попытка 1
11:01:35.931 LOAD не взяли (IDLE/ERROR) — гружу заново, ещё 296 с
11:01:44.028 упаковка с 0.0 с                                  ← второй прогон, живой
11:01:46.050 играю … s1e6 — на ТВ
```

#### 🔴 Пункт 4 закрыт: умирала не та упаковка, и убивали её мы сами

Дефект читался как «первый прогон упаковки следующей серии умирает через 0.23 с молча».
Обе половины формулировки оказались неверны, и разобрал их не разговор, а журнал того же
прогона с `TORRCAST_TRACE=1` — вот эти две строки в **112 микросекундах** друг от друга:

```
11:01:32.159938 упаковка оборвалась (нет вывода) — начинаю заново, попытка 1
11:01:32.160050 запрос v0.ts · ждал 0.0 с · 404
```

**Строка смерти ffmpeg такая: её нет.** Проверено на стенде отдельным опытом: ffmpeg,
получивший SIGTERM, выходит **кодом 255** — положительным, то есть на «убит сигналом» не
похожим, — а прощание «Exiting normally, received signal 15» пишет уровнем `info`, которого
при нашем `-loglevel warning` не видно. Молчание stderr было не загадкой, а подписью нашего
же `Packer.stop()`. Гипотеза «дать ffmpeg говорить в файл» лечила бы не ту болезнь: журнал
прогона (`tempfile.TemporaryFile`) и так переживает процесс, писать в него было некому.

**Мгновенный 404 сказал, чей это был труп.** Пустой ответ за 0.0 с даёт ровно одна ветка —
`Feed.fatal`, то есть раздача, у которой уже вызвали `stop()`. Значит `v0.ts` следующей
серии спрашивали **не у новой раздачи, а у прошлой**, и ffmpeg, на который ругался показ,
был ffmpeg прошлой серии, снятый нами же секундой раньше. Новая упаковка в это время была
жива и здорова.

Причина — **HTTP keep-alive**: раздача идёт по HTTP/1.1, приёмник держит одно соединение на
весь показ, а `server_close()` закрывает только слушающий сокет — потоки-обработчики
демонические, и `block_on_close` их не ждёт. На стыке серий LOAD уходил в то самое живое
соединение прошлой серии: манифест приезжал прошлый, `v0.ts` — 404, приёмник отвечал
`IDLE/ERROR`, дальше шло штатное лечение (`_restart_app` + пауза + переподключение). Вот эти
15–16 с и видел владелец. На одиночном фильме дефекта нет ровно потому, что там нет второго
показа, который спросил бы у остановленной раздачи.

Починено:

* `HlsServer.stop()` закрывает **живые соединения**, а не только слушающий сокет: показ,
  который остановлен, обязан замолчать, а не досказывать прошлую серию в keep-alive;
* `Packer` помнит, что его сняли мы (`stopped`), и `why()` больше не выдаёт наш SIGTERM за
  аварию; молчание с положительным кодом печатается как «молча, код 255», а не «нет вывода»;
* `Feed._steer` проверяет `fatal` **первым**: у остановленной раздачи диагностировать
  нечего, и попытку перезапуска чужой запрос у неё больше не отнимает.

| стык серий s1e5 → s1e6 (живой Q70D) | до | после |
|---|---|---|
| пустой экран (от «следующая серия» до картинки) | 15.5 с | **~3.0 с** (ffprobe длительности 2.0 + LOAD 1.0) |
| «старт NN с» собственного секундомера серии | 14 с | **1 с** |
| 404 за стык | 4 (все на `v0.ts`) | **0** |
| «упаковка оборвалась», «LOAD не взяли», «приёмник залип» | по одному разу | **ни одного** |

Замер 06-08-2026, 12:01–12:05: `s1e5` с 23:00, автопереход на пороге 95 %, две минуты
`s1e6` — позиция шла монотонно, ребуферов не было, состояние владельца бралось из копии и
не менялось.

**Что осталось от §7.4:** пункт 7 (🔵 мелочи).

## 7.5 Короткие смоки на живом Q70D (06-08-2026, день)

Девять пунктов чек-листа, каждый — настоящий `cast` под pty на стенде, приёмник
`chromecast`, ТВ 192.168.100.102. Длинные прогоны (полный фильм, серия целиком) сюда не
входят — они за владельцем (§1, поправка 05-08).

| # | смок | факт |
|---|---|---|
| 1 | `cast моана 2` холодный TorrServer | «релиз №1 на деле 574p — беру №2 (настоящий 1080p)» → `1080p`; **старт 17 с** от команды (Enter → картинка 8.6 с); `duration` в MEDIA_STATUS = 1:39:37 |
| 2 | `cast status` во время показа | `играю «Моана 2» · 1080p — 0:00:20 / 1:39:37` — совпадает с журналом показа |
| 3 | перемотки владеющим сендером | вперёд в неупакованное 6.1 с, назад в окне 4.0 с (без перепаковки), назад глубже окна 8.1 с → **6.1 с** после починки; **404 за прогон — 0** |
| 4 | пауза 80 с | на 60-й секунде «пауза на пульте — упаковку гашу», `play` → PLAYING за 0.04 с, позиция та же |
| 5 | `cast stop` → повтор | «остановлено … на 0:35:03»; юнит `success`/код 0; приложение приёмника пусто сразу; tmpfs 0 файлов; `cast моана 2` → «Продолжить?» → Enter → картинка **7.3 с** (упаковка 4.1 + телевизор 3.1) |
| 6 | `cast моана` + голый Enter | список 1926/2016/2024/2026, дефолт `[2]` → «Моана» (2016) · 1080p · Дубляж, подмены нет; Enter → картинка 7.5 с; 404 — 0 |
| 7 | стык серий s1e5 → s1e6 | автопереход состоялся, s1e6 играет; соединений к ТВ 2 → **1**; пустой экран 15.3 → 16.1 с, после починки keep-alive — **~3 с** (🔴 закрыт, §7.4) |
| 8 | Ctrl-C на «Что смотрим?» | код 2, **раздач в TorrServer 0** |
| 9 | mp4 (YTS 1.8 ГБ) — впервые на ТВ | `Moana.2.2024.1080p.WEBRip.x264.AAC5.1-[YTS.MX].mp4` · 1080p; LOAD→PLAYING **1.4 с**; минута показа без единого BUFFERING; перемотка вперёд и глубоко назад — картинка не гасла вовсе, расхождение с манифестом **0.000 с**, 404 — 0, tmpfs 46 МБ |

Что живой прогон показал сверх чек-листа:

* 🟠 **Честный 1080p «Моаны 2» (13.3 ГБ, 17.8 Мбит/с) идёт с ребуферами.** Раз в 30–60 с
  приёмник встаёт на 8–10 с при 60–170 с готовой упаковки впереди и полосе раздачи
  270–475 Мбит/с, после чего его расшевеливает нудж — и каждый такой нудж **пропускает
  8 с фильма**. «Моана» 2016 (1080p, вдвое легче) и mp4 от YTS на том же ТВ идут без
  единого ребуфера. Это ровно потолок декодера Q70D из §9 (между 18 и 22 Мбит/с), и
  теперь в него упирается сам выбор «честного качества» (§7.4). **Решение владельца
  06-08-2026: потолок `bitrate_warn_mbit` опущен 20 → 16, смотрибельность важнее пиковой
  чёткости.** Одного порога мало: до ffprobe длительности картины не знает никто, и
  прикидка делит 13.3 ГБ на типовые два часа — 14.8 Мбит/с, то есть под потолком. Поэтому
  потолок проверяется ещё раз по прочитанному файлу (`_Bench._trouble`). На живой выдаче
  «моана 2»: «релиз №2 не годится (тяжёлый, ~19 Мбит/с)» → дефолт уходит на настоящий 720p
  (7.6 ГБ, ~10 Мбит/с). Руками тяжёлый берётся как и прежде: `--release 2` играет его и
  печатает «внимание: ~19 Мбит/с — ресивер на таком битрейте может встать». Порогов
  сторожа (§6.0) правка не касается.
* 🟡 **tmpfs на тяжёлом релизе доходил до 691 МБ** (на «Моане» 2016 — 120 МБ, на mp4 —
  46 МБ). Роста без предела нет: потолок задаёт `burst` (запас впереди ~180 с), а после
  каждой перемотки уборка вперёд сбрасывает вес втрое (411 → 78 МБ). На стенде
  `/dev/shm` 63 ГБ, но число стоит помнить.
* LOAD→PLAYING на тяжёлом mkv — 3.1–4.6 с против 0.7–1.5 с, замеренных 05-08 на лёгком
  релизе, и 1.4 с на mp4. То есть «жду телевизор» в старте — это в основном первый
  сегмент на 30 МБ, а не приёмник.

## 7.6 Регресс с включённым перекодированием на живом Q70D (06-08-2026, ночь)

Короткие смоки (1.5–4 мин), настоящий `cast` под pty, приёмник `chromecast`, ТВ
192.168.100.102, наблюдение владеющим сендером (`TORRCAST_TRACE`, опрос 2 с). Состояние
владельца не тронуто: у смоков свой `TORRCAST_STATE` с прибитым релизом.

| # | смок | факт |
|---|---|---|
| 1 | лёгкий фильм, «Моана» 2016 с нуля | старт **4 с**; **2 опроса BUFFERING из 26** (оба на прогреве); перекод не мешает — тяжёлыми оказались 4 куска из 641 (всплески), tmpfs 23→67 МБ |
| 2 | «Моана 2» 13.3 ГБ, «Продолжить?» с 1:21:24 (тяжелейшее место, v358 22 Мбит/с) | старт **6–10 с**, голова перекодирована (21 МБ вместо 43); **0 опросов BUFFERING из 50**, позиция ровно +2 с на опрос, ни одного нуджа. Тот же смок с `recode_head_wait=0` — 8 из 38 |
| 3 | перемотка ИЗ тяжёлого в лёгкое (4900 → 5904 с) | упаковка с 5894.1 (докатка 10.0), голова v441 лёгкая — не держали, картинка сразу; 404 — 0 |
| 4 | перемотка В тяжёлое (5904 → 3949 с, v299 29 Мбит/с) | упаковка с 3948.8, голова **перекодирована за 8 с** (`ultrafast`), дальше очередь как есть (v300, v305, v307); 404 — 0 |
| 5 | сериал, s1e5 (лёгкий) | «тяжёлых кусков нет — перекодировать нечего» — поток кодировщика **не поднимается вовсе**; старт 4 с, **0 BUFFERING из 23**, tmpfs 28–30 МБ |
| 6 | стык серий s1e5 → s1e6 с включённым перекодом | автопереход состоялся, s1e6 играет, старт следующей серии **1–2 с**; у s1e6 тяжёлых 13 из 163 (8 %) — кодировщик поднялся и не помешал |
| 7 | `cast stop` | «остановлено … на 0:01:37»; юнит `inactive`; tmpfs **0 файлов, 0 байт**; каталог `recode/` **убран вместе с остальным** |
| 8 | tmpfs на тяжёлом релизе | пик **232 МБ** (было 691 МБ без перекода, §7.5): перекодированные куски втрое легче копий, и запас впереди весит меньше |

⚠️ Что смоки НЕ покрыли: полный фильм, пауза 80 с, `cast status`, Ctrl-C на вопросе, mp4 —
всё это в §7.5 проверялось до перекодирования, и правка §6.2.1 их не касается ни одной
строкой (голова живёт внутри `Recoder`, выкладка осталась в `Packer.publish`).

## 8. Что НЕ делаем в v2

- Не переписываем парсер, поиск, кластеризацию франшиз, state — они доказаны на корпусе
  21 540 имён и живой выдаче.
- Не трогаем kinocast (владелец им пользуется), amnezia, прод-ингресс, PBS.
- Не заводим телеграм-бота, веб-морду, голос — как и в v1.
- Не решаем вопрос индексеров через zapret, пока владелец не скажет: поиск на шиме к Knaben
  работает, RuTor — приятное дополнение, а не требование.

## 9. Известные ограничения, которые остаются

- Физический экран ни один агент не видел: доказательства — состояние приёмника, забор
  сегментов, `status_text`. ⚠️ `is_stand_by=True` и `volume_level=0.0` этот Samsung отдаёт
  всегда, как индикаторы они бесполезны. Финальное «вижу картинку» — за владельцем.
- HEVC на Q70D по-прежнему неизвестен: дефолт всегда H.264 по реальному `ffprobe`.
- ~~Потолок битрейта ~20 Мбит/с (порог декодера между 18 и 22 по опыту kinocast).~~
  🔴 Опровергнуто замером 06-08-2026 вечером (§6.2): Q70D встаёт в BUFFERING на «Моане 2»
  13.3 ГБ уже при 13–15 Мбит/с, и перекодирование в 12 его не спасает. Рабочая планка,
  снятая на живом ТВ, — перекодировать всё выше 10 Мбит/с в 9.
  ✅ Планка **подтверждена вторым релизом** ночью 06-08 (§6.2.1): «Тачки 3» 13.0 ГБ,
  видео 14.10 Мбит/с, 1920×804, другой энкодер — 36 опросов BUFFERING из 50 без перекода.
  Механизм по-прежнему не объяснён, но искали честно: паспорта ffprobe пяти файлов (потоки,
  формат, покадровые окна, разбор SPS) **дискриминатора не дали** — у чистого YTS-mp4 та же
  геометрия 1920×960 и почти тот же SPS, что у проблемного. Единственное, что делит набор, —
  битрейт видеодорожки. ⚠️ «refs=1» тут было ошибкой чтения: `ffprobe` отдаёт `refs: 1`
  для всех файлов, настоящее значение — только из SPS (у проблемного 4, у чистого 5).
  Число снято на двух релизах и одном телевизоре.
