"""Последние рубежи ожидания ВНУТРИ юнита показа: метаданные и длительность.

Отличать их от бюджетов фазы под меню (:mod:`torrcast.domain.pick_settings`) важно:
здесь магнит юниту уже отдан, и не приехало - значит показывать нечего.
"""

from typing import Final

#: Потолок ожидания метаданных раздачи **в юните**, секунды. Здесь это не «бюджет фазы
#: под меню» (:data:`torrcast.domain.pick_settings.META_BUDGET`), а последний рубеж: магнит юниту уже
#: дали, и если метаданные не приехали, показывать нечего.
WORKER_META: Final = 60.0

#: Потолок ffprobe длительности в юните: своей длительности следующая серия не знает, и
#: читается она из потока (:func:`_duration`).
WORKER_DUR: Final = 90.0
