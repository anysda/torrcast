"""English captions of the port-slot cluster."""

from __future__ import annotations


def en() -> dict[str, str]:
    """Return the English catalog of the port-slot cluster."""
    return {
        "ports.show_unit_not_installed": "no show unit assigned: the app is not assembled",
        "ports.state_store_not_installed": "no state store assigned: the app is not assembled",
    }
