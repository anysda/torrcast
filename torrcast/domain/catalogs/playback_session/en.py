"""English captions of the playback session cluster."""

from __future__ import annotations


def en() -> dict[str, str]:
    """Return the English catalog of the playback session cluster."""
    return {
        "playback_session.stream_address_unknown": "stream address is not known",
    }
