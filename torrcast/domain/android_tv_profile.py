"""Измеренный профиль приставки Android TV."""

from typing import Final

from torrcast.domain.receiver_profile import ReceiverProfile
from torrcast.domain.segment_container import FMP4

__all__ = ["ANDROID_TV"]

ANDROID_TV: Final = ReceiverProfile(
    key="androidtv",
    title="приставка Android TV (Xiaomi TV Stick)",
    segment_container=FMP4,  # снято: НЕ НАЗВАН · неизвестен · 24-08 · CMAF
    recode_codecs=frozenset({"mpeg4"}),  # снято: НЕ НАЗВАН · неизвестен · 24-08 · CMAF
    copy_codecs=frozenset({"h264", "hevc", "vp9"}),  # снято: НЕ НАЗВАН · неизвестен · 24-08 · CMAF
    copy_10bit_codecs=frozenset({"hevc"}),  # снято: НЕ НАЗВАН · неизвестен · 24-08 · CMAF
    # Белый список относится к измеренному CMAF-тракту, а не к паспорту декодера:
    # HEVC 8/10 бит и VP9 проходят копией с init-сегментом и подсказкой LOAD ``fmp4``.
    # Hi10P и AV1 через этот тракт отвергаются и остаются вне списка копии.
    # Живой замер на приставке (TC-620). У релиза, на котором мерили, индекс Cues врун,
    # карта опорных кадров отвергается, и сетка выходит ровная по 10 с: куски копии
    # 19.1-19.9 МБ. С осторожными 16 МБ наружу не выходит НИ ОДИН кусок: на ровной сетке
    # профиля тяжести нет, ужимать нечем, и выкладка честно пропускает каждый (39 из 39),
    # а показа нет вовсе - три прогона из трёх. С 28 МБ те же куски уезжают копией: КПД
    # 0.992-0.997 за 240 с показа, ни одной остановки, четыре прогона из четырёх, включая
    # прогон с пустой полки прогретого. Прощупан и сам потолок: куски 26.9-27.7 МБ (ровная
    # сетка по 14 с) приставка доигрывает 87 с подряд без единого подвиса.
    # У Q70D его 16 МБ остаются нетронутыми: это число снято на другом приёмнике.
    # снято: tvprobe · mpegts · TC-620
    max_segment_bytes=28_000_000,  # снято: tvprobe · mpegts · TC-620
    # Живой замер окна запроса на приставке (TC-756): она просит следующий кусок, когда
    # впереди остаётся 20.0 с плёнки - медиана по 451 запросу двух прогонов, десятая доля
    # 19.8 с, самое тесное наблюдение 19.4 с. Кусок такой же длины оставляет её ровно с
    # одним куском в запасе, и на границе после него приставка САМА встаёт на паузу: 15 из
    # 15 её самопауз за два прогона по 13 минут пришлись на куски 19.7-27.9 с, и ни одной
    # на куски короче. По ленте показа то же: 9 подгрузов из 27 таких границ против 1 из
    # 179 остальных, а в архиве приёмки - 6 из 31 против 0 из 216.
    # 15 с - это середина промежутка между самым коротким сломавшимся куском (19.2 с) и
    # самой длинной проверенной живьём чистой нарезкой (14.5 с, 13 минут без единой
    # самопаузы): запас 4.2 с вниз от поломки и 4.4 с вниз от самого тесного окна.
    # снято: tvprobe · mpegts · TC-756
    max_segment_seconds=15.0,  # снято: tvprobe · mpegts · TC-756
    warn_mbit=28.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    recode_at_mbit=28.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    # Верх цели - тот же измеренный поток, который приставка принимает нативно.
    # Длина куска и потолок 28 МБ опускают фактическую цель через Encode.fit.
    # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    recode_mbit=28.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    # 🔴 Пороги сторожа подвиса (``stall_seconds``, ``ready_ahead``, ``stall_skip``,
    # ``blind_nudges``) тут НЕ переопределены, и это замер, а не недосмотр (TC-728).
    # Сторож - самсунговский: вход в него ровно один, состояние ``BUFFERING``, а этот
    # приёмник называет себя играющим всё время, пока картинка стоит, - и на нём он не
    # зовётся вовсе. Три прогона по 8 минут показа: самопауз по собственному журналу
    # приёмника 0, ``DEMUXER_UNDERFLOW`` 0, входов в ``BUFFERING`` 1 (и тот на старте),
    # нуджей 0. Настраивать эти числа тут не на чем, и трогать их без своего события
    # нельзя: они сняты на другом приёмнике.
    #
    # Второго входа - по неподвижному указателю - сторож не получил намеренно, и это тоже
    # замер. Самый долгий простой указателя за те же три прогона - 4.0 с при пороге входа
    # 8.0 с, то есть второй вход не сработал бы ни разу; опущенный под него порог покупал
    # бы 2-3 с стоящей картинки ценой прыжка в ``stall_skip`` или через границу куска -
    # 8.0 и 10.0 с соответственно. Подталкивание видно зрителю скачком картинки, и платить
    # десятью секундами плёнки за две простоя незачем.
    # снято: НЕ НАЗВАН · неизвестен · TC-728
    patience=577.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    app_patience=577.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    dead_url_seconds=4.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    segment_retries=0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    revive_timeout=577.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    revive_pause=10.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    revive_drop=10.0,  # снято: НЕ НАЗВАН · неизвестен · 09-08 · замер
    # Свежий рой на приставке ещё разгоняется: в трёх живых раздачах рабочий темп
    # появился через 4.5, 9.5 и 4.5 с. До 10-й секунды короткий прогрев не даёт
    # честного окна, а молчание меры не может быть приговором хорошей раздаче.
    # Осторожное нулевое умолчание относится к другому приёмнику и не меняется.
    # снято: НЕ НАЗВАН · не при чём · 24-08 · рой
    supply_settle_seconds=10.0,  # снято: НЕ НАЗВАН · не при чём · 24-08 · рой
)
