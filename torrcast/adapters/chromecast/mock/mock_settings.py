"""Числа сухого приёмника, не зависящие от модели: срок подъёма погасшего показа.

Наследует их :class:`torrcast.adapters.chromecast.mock.mock_receiver.MockReceiver`."""

from __future__ import annotations


class _Settings:
    """Порог, который от модели приёмника не зависит.

    🔴 Терпение к стоящей картинке, перезаборы куска и обида на 404 у моделей разные и
    живут в профиле приёмника (:mod:`torrcast.domain.profile`): показ спрашивает
    ``self.profile``, а не этот класс.
    """

    #: Сколько ждём картинку, поднимая погасший показ (:meth:`replay`) - как у живого
    #: приёмника: попытка тут не одна, интервалы держит зовущий.
    WAKE_TIMEOUT = 60.0
