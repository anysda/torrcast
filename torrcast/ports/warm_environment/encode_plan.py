"""План перекода куска: прогрев его возит, а читает сборка команды ffmpeg."""

from typing import Protocol


class EncodePlan(Protocol):
    """Чем перекодировать тяжёлый кусок: прогрев этот план ВОЗИТ, но не читает.

    Ручек тут не названо ни одной, и это сам договор, а не недосказанность. План
    приезжает в прогрев готовым (его собирает отбор по профилю приёмника) и уходит
    прямиком в сборку команды упаковщика; всё, что из него достают - аргументы видео, -
    достаёт она. Пустое имя вместо ``Any`` тем и ценно: оно возит план через границу и
    при этом ЗАПРЕЩАЕТ звать у него что-либо по пути.
    """
