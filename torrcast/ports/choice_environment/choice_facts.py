"""Фоновая справка о картинах в объёме, который нужен пересборке плана."""

from typing import Protocol

from torrcast.domain.facts.fact import Fact


class ChoiceFacts(Protocol):
    """Справка, которую меню уже дождалось: по названию картины - её факты.

    Лишнего запроса тут нет ни одного: хронометраж приезжает в той же справке, которую
    меню уже показало человеку. Справка молчит - отдаёт пустой :class:`Fact`, а не
    ``None``: «не знаю» тут решение, а не отказ.
    """

    def get(self, title: str, year: int | None) -> Fact: ...
