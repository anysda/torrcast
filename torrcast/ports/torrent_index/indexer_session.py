"""Сессия индексеров: её заводят на весь поиск и дальше только возят по вызовам."""

from typing import Protocol


class IndexerSession(Protocol):
    """Сессия индексеров: её ЗАВОДЯТ и ПЕРЕДАЮТ обратно, но никогда не читают.

    Полей тут не названо ни одного, и это сам договор, а не недосказанность. Сессию
    открывают один раз на весь поиск (:meth:`ProwlarrApi.open`) и дальше только носят
    из вызова в вызов; всё, что с ней делают - соединения, заголовки, повторы, - знает
    ровно тот адаптер, который её завёл. Пустое имя вместо ``Any`` тем и ценно: оно
    возит сессию через границу и при этом ЗАПРЕЩАЕТ звать у неё что-либо по пути.
    """
