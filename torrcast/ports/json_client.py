"""Запрашивает JSON у внешнего HTTP-источника."""

from typing import Protocol

from torrcast.ports.json_value import JsonValue


class JsonClient(Protocol):
    """Выполняет один GET-запрос с заданным таймаутом и умеет греть имя заранее."""

    def get(
        self,
        host: str,
        path: str,
        params: dict[str, str],
        headers: dict[str, str],
        timeout: float,
    ) -> JsonValue: ...

    def warm(self, host: str) -> None:
        """Начать разрешение имени, не дожидаясь ответа: адрес понадобится позже.

        🔴 TC-957. Второй волне справки нужен хост, о котором до неё никто не спрашивал
        (:func:`~torrcast.adapters.wiki.wiki_host.wiki_host`), и разрешается он В ЕЁ
        СРОК - на стенде это стоило английскому меню всей справки: к секунде второй волны
        по сети уже колотит прогрев раздач, имя не разрешалось за 1.2 с ни разу, а первая
        волна тем же резолвером за те же полторы секунды до прогрева укладывалась в 0.18 с.
        Адрес известен заранее - вот его и спрашивают заранее, пока сеть свободна.
        """
