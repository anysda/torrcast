"""Клиент индексеров: один на весь поиск вместе с доборами."""

from typing import Protocol


class IndexerClient(Protocol):
    """Клиент индексеров: сценарий добора его ВОЗИТ, но ни одной ручки у него не зовёт.

    Ручек тут не названо ни одной, и это сам договор, а не недосказанность. Клиент
    заводится один на весь поиск вместе с доборами - со своим бюджетом и своим счётом
    молчунов, - а сценарий передаёт его кругу поиска
    (:func:`torrcast.usecases.discover._ask`) и охраннику остатка цели
    (:func:`torrcast.usecases.discover._no_budget`): договор с ним держат они. Пустое имя
    вместо ``Any`` тем и ценно: оно возит клиента через границу и при этом ЗАПРЕЩАЕТ
    звать у него что-либо по пути.
    """
