"""Опоздавших нет: план собран не поиском, и ждать ему некого."""

from __future__ import annotations


def _nobody_waiting() -> tuple[str, ...]:
    """Опоздавших нет: план собран не поиском (тесты, отладочные ручки) - ждать некого."""
    return ()
