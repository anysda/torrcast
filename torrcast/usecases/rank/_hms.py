"""«ч:мм:сс» под прежним адресом отбора: сама единица живёт в предметной области.

Адрес держится ради :mod:`torrcast.usecases.select`, который зовёт этот модуль
напрямую, и ради самого пакета отбора: имя подпакета обязано быть занято функцией,
иначе первый же импорт этого модуля затрёт её собой в пространстве пакета.
"""

from torrcast.domain._hms import _hms as _hms
